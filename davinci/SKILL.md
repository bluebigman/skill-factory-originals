---
slug: davinci
name: davinci
displayName: 数据解析 可视化 批量转换
description: 将数据文件URL解析为结构化结果，支持批量处理与自定义格式输出。
version: 1.0.0
license: MIT
source_project: original
source_url: 
copyright_holder: 原创作者（自持版权）
ai_generated: true
ai_tools: ["DeepSeek"]
disclaimer: 本Skill由AI辅助生成，提供使用指导和最佳实践。使用前请阅读相关文档。
author: 数据工坊
agent_created: true
trigger_words: ["数据可视化", "数据解析", "图表生成", "数据转换", "可视化服务", "文件解析", "批量处理"]
---

> 本内容由 AI 生成，仅供学习参考
<!-- ai-generated-notice -->

# davinci — 数据文件解析与可视化转换器

## 一、能力边界（一页纸速查卡）

### 1.1 能做什么

| 能力项 | 说明 | 示例 |
|--------|------|------|
| URL 解析 | 将用户提供的文件 URL 解析为结构化数据 | `https://example.com/data.csv` → JSON 数组 |
| 批量处理 | 对同一目录下多个文件执行相同解析逻辑 | 解析 `./input/*.csv` 全部文件 |
| 自定义格式输出 | 支持 JSON、CSV、Markdown 表格三种输出格式 | `--format json` / `--format csv` / `--format md` |
| 数据可视化 | 将解析结果生成基础图表（柱状图、折线图、饼图） | 输出 `chart.png` 文件 |
| 字段映射 | 支持自定义字段名映射与类型转换 | `--map "date:日期,value:数值"` |

### 1.2 不能做什么

- 不能解析加密或受密码保护的文件
- 不能处理超过 500MB 的单个文件（内存限制）
- 不能自动识别无表头且无分隔符的纯文本文件
- 不能生成交互式动态图表（仅静态图片）
- 不能对解析结果进行业务逻辑判断（如"是否合规"）

### 1.3 适用对象

- 需要快速将 CSV/JSON/TSV 文件转为可视化图表的分析师
- 需要批量处理同构数据文件的运维人员
- 需要将数据文件 URL 转为结构化接口数据的开发者

---

## 二、触发方式

### 2.1 触发词

当用户输入包含以下任一词汇时，本 Skill 将被激活：

- 数据可视化、数据解析、图表生成、数据转换、可视化服务
- 文件解析、批量处理、URL 转结构、数据清洗

### 2.2 场景映射表

| 用户说（大白话） | 实际需求 | 触发动作 |
|------------------|----------|----------|
| "帮我把这个 CSV 变成图表" | 解析 CSV 并生成可视化图表 | 解析 + 图表生成 |
| "这堆文件都转成 JSON" | 批量格式转换 | 批量解析 + 格式输出 |
| "这个链接里的数据能看吗" | 验证 URL 可解析性 | 试运行模式 |
| "把日期字段改成时间戳" | 字段类型转换 | 字段映射 + 类型转换 |

---

## 三、标准流程

### 3.1 前置条件

| 条件 | 要求 | 检查方式 |
|------|------|----------|
| 文件可访问 | URL 可下载或本地路径存在 | `curl -I <url>` 返回 200 |
| 格式支持 | 文件扩展名为 .csv/.json/.tsv | `file <path>` 确认 MIME 类型 |
| 命名规范 | 批量文件需遵循 `prefix_*.ext` 模式 | `ls ./input/` 查看 |
| 内存充足 | 单文件 ≤ 500MB | `ls -lh <file>` 查看大小 |

### 3.2 执行步骤

#### 步骤 1：准备输入

将待处理文件放入同一目录（如 `./input/`），确认命名规范一致。

```bash
mkdir -p input output
cp /path/to/files/*.csv ./input/
```

#### 步骤 2：试运行（单样本验证）

先用单个文件执行，核对输出字段与格式是否符合预期：

```bash
davinci parse ./input/sample.csv --format json --output ./output/sample.json
```

检查输出文件中的字段名、数据类型、行数是否与源文件一致。

#### 步骤 3：批量执行

确认无误后，对全量数据执行：

```bash
davinci batch ./input/*.csv --format json --output ./output/
```

> ⚠️ 执行前自动备份原始文件至 `./backup/` 目录，防止误操作。

#### 步骤 4：校验结果

抽查输出条目，核对关键字段与源数据一致：

```bash
davinci verify ./output/sample.json --source ./input/sample.csv
```

校验规则：
- 行数一致（±0 容差）
- 关键字段（如 ID、日期）无空值
- 数值字段类型正确（int/float）

### 3.3 输出规范

| 输出格式 | 文件扩展名 | 编码 | 备注 |
|----------|------------|------|------|
| JSON | .json | UTF-8 | 数组结构，每元素为一条记录 |
| CSV | .csv | UTF-8 | 首行为表头，逗号分隔 |
| Markdown | .md | UTF-8 | 表格形式，含表头与分隔线 |
| 图表 | .png | — | 默认 800×600 分辨率 |

---

## 四、置信度门控

当遇到以下情况时，**不编造数据**，输出 `[需核实:字段]` 占位符：

| 场景 | 处理方式 |
|------|----------|
| 源文件某字段缺失 | 输出 `[需核实:字段名]` |
| 数值解析失败（非数字） | 输出 `[需核实:数值]` |
| 日期格式无法识别 | 输出 `[需核实:日期]` |
| URL 下载超时 | 中止任务，提示用户重试 |

示例：

```json
{
  "id": 1001,
  "name": "产品A",
  "price": "[需核实:price]",
  "date": "[需核实:date]"
}
```

---

## 五、错误码体系

| 错误码 | 含义 | 提示话术 | 修正步骤 |
|--------|------|----------|----------|
| E001 | 文件不存在 | "未找到指定文件，请检查路径" | 确认路径正确，使用 `ls` 查看 |
| E002 | 格式不支持 | "不支持该文件格式，仅支持 CSV/JSON/TSV" | 转换文件格式后重试 |
| E003 | 文件过大 | "文件超过 500MB 限制" | 拆分文件或使用流式处理 |
| E004 | 解析失败 | "解析过程中出现错误，请检查文件内容" | 查看错误日志，定位行号 |
| E005 | 输出目录不可写 | "无法写入输出目录，请检查权限" | 修改目录权限或更换路径 |
| E006 | URL 下载失败 | "无法从 URL 下载文件，请检查网络" | 确认 URL 可访问，重试 |

---

## 六、FAQ 反模式

### 6.1 常见坑

| 坑 | 反模式（错误做法） | 正模式（正确做法） |
|----|-------------------|-------------------|
| 跳过试运行 | 直接批量处理所有文件 | 先用单个样本验证字段映射 |
| 忽略备份 | 直接覆盖原始文件 | 保留 `./backup/` 备份 |
| 字段名硬编码 | 假设所有文件字段名一致 | 使用 `--map` 参数显式映射 |
| 忽略空值 | 空值直接丢弃 | 用 `[需核实:字段]` 占位 |
| 格式混用 | 一个目录混放 CSV 和 JSON | 分目录存放，分别处理 |

### 6.2 反模式对照

**反模式 1**：用户说"全部转成 JSON"，直接执行批量命令，结果发现部分文件字段名不同，输出混乱。

**正模式**：先执行 `davinci parse ./input/sample.csv --format json` 验证，再批量执行。

**反模式 2**：用户提供 URL 后直接解析，未检查文件大小，导致内存溢出。

**正模式**：先 `curl -I <url>` 检查 Content-Length，确认 ≤ 500MB 再解析。

---

## 七、渐进式披露

### 7.1 速查卡（30 秒上手）

```
1. 放文件 → ./input/
2. 试运行 → davinci parse ./input/sample.csv --format json
3. 批量 → davinci batch ./input/*.csv --format json --output ./output/
4. 校验 → davinci verify ./output/sample.json --source ./input/sample.csv
```

### 7.2 分层次阅读路径

**新手路径**（首次使用）：
1. 阅读「能力边界」了解能做什么
2. 按「标准流程」步骤 1-2 完成单文件解析
3. 使用 `--help` 查看所有参数

**进阶路径**（熟练用户）：
1. 阅读「错误码体系」排查问题
2. 使用 `--map` 自定义字段映射
3. 使用 `--chart` 生成可视化图表
4. 结合 `--format md` 输出 Markdown 报告

---

## 八、参数参考表

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--input` | string | 必填 | 输入文件路径或 URL |
| `--output` | string | ./output/ | 输出目录或文件路径 |
| `--format` | string | json | 输出格式：json/csv/md |
| `--map` | string | 无 | 字段映射，如 `"id:ID,name:名称"` |
| `--chart` | string | 无 | 图表类型：bar/line/pie |
| `--batch` | boolean | false | 批量模式，输入为目录 |
| `--verify` | boolean | false | 校验模式，对比源文件 |
| `--selftest` | boolean | false | 自检模式，验证安装 |
| `--version` | boolean | false | 显示版本号 |

---

## 九、用户协议

<!-- user-agreement-injected -->

**使用 davinci Skill 即表示您同意以下条款：**

1. **责任承担**：使用者自行承担使用本 Skill 产生的全部责任。因使用、误用或依赖本 Skill 导致的任何直接或间接损失，作者不承担任何责任。
2. **禁止反向工程**：不得对本 Skill 进行反向工程、反编译、破解或试图提取源代码逻辑。
3. **数据安全**：使用者需自行确保输入数据的合法性与安全性，不得上传违反法律法规的内容。
4. **无担保**：本 Skill 按"原样"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性。
5. **修改与分发**：未经许可，不得修改、再分发本 Skill 的完整或部分内容。

---

## 十、许可证（License）

<!-- professional-license-embedded -->

**MIT License**

版权所有 (c) 2025 数据工坊

特此免费授予任何获得本软件及相关文档文件（以下简称"软件"）副本的人士处理本软件的权利，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或销售软件副本的权利，并允许向提供软件的人士授权这样做，但须满足以下条件：

上述版权声明和本许可声明应包含在软件的所有副本或实质性部分中。

本软件按"原样"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性保证。在任何情况下，作者或版权持有人均不对任何索赔、损害或其他责任负责，无论是在合同诉讼、侵权行为或其他方面，由软件或软件的使用或其他交易引起、产生或与之相关。

---

*本 Skill 由 AI 辅助生成，仅供参考。使用前请阅读相关文档并自行验证输出结果。*
