---
slug: youtube-transcript-api-sharp
name: youtube-transcript-api-sharp
displayName: 字幕转录 结构化解析 字段校验
description: 解析YouTube字幕数据，按规范输出结构化转录结果。
version: 1.0.0
license: MIT
source_project: original
source_url: 
copyright_holder: 原创作者（自持版权）
ai_generated: true
ai_tools: ["DeepSeek"]
disclaimer: 本Skill由AI辅助生成，提供使用指导和最佳实践。使用前请阅读相关文档。
author: skill-forge-lab
agent_created: true
trigger_words: ["视频字幕", "youtube transcript api sharp", "字幕转录", "转录解析", "字幕提取", "字幕结构化", "转录字段校验"]
---

> 本内容由 AI 生成，仅供学习参考
<!-- ai-generated-notice -->

# YouTube 字幕转录解析 Skill 文档

## 一、能力边界（一页纸速查卡）

### 1.1 能做什么

| 编号 | 能力项 | 说明 |
|------|--------|------|
| C1 | 字幕文件解析 | 读取 YouTube 字幕原始数据（JSON/SRT/VTT 格式），提取文本内容 |
| C2 | 结构化输出 | 将非结构化字幕转换为包含时间戳、文本、序号等字段的规范记录 |
| C3 | 批量处理 | 支持多文件批量解析，输出统一格式的结果集 |
| C4 | 字段校验 | 对输出结果的关键字段（时间码、文本完整性）进行一致性核验 |
| C5 | 格式转换 | 在 SRT、VTT、JSON 三种常见字幕格式间进行转换输出 |

### 1.2 不能做什么

| 编号 | 限制项 | 说明 |
|------|--------|------|
| L1 | 不进行语义翻译 | 仅解析转录，不提供翻译服务 |
| L2 | 不处理视频流 | 仅处理字幕文本数据，不涉及音视频解码 |
| L3 | 不自动生成字幕 | 若源数据无字幕轨道，无法凭空生成 |
| L4 | 不修正原文字错误 | 保留源字幕的原始文本，不做拼写纠错 |
| L5 | 不处理加密字幕 | 仅支持公开可访问的字幕数据 |

### 1.3 适用对象

- 需要将 YouTube 字幕转为结构化文本的研究人员
- 需要批量整理字幕语料的内容运营人员
- 需要从字幕中提取时间轴信息的开发者
- 需要将字幕用于 NLP 训练数据集的工程师

---

## 二、触发方式

### 2.1 触发词速查

| 触发场景 | 推荐触发词 | 示例指令 |
|----------|------------|----------|
| 解析单个字幕文件 | 字幕转录 / 转录解析 | "帮我转录这个字幕文件" |
| 批量处理字幕 | 字幕提取 / 批量转录 | "把这个目录下所有字幕都解析了" |
| 结构化输出 | 字幕结构化 / 字段校验 | "按规范输出转录结果" |
| 格式转换 | 字幕转换 / 格式转换 | "把 SRT 转成 JSON" |

### 2.2 场景映射表

| 用户真实需求 | 大白话表达 | 本 Skill 响应 |
|-------------|-----------|---------------|
| 想从字幕中提取纯文本 | "把字幕里的文字都抠出来" | 输出纯文本字段，去除时间码 |
| 想按时间定位内容 | "我想知道某句话在视频的什么位置" | 输出带时间戳的结构化记录 |
| 想批量整理字幕文件 | "我有一堆字幕文件要整理" | 批量解析并输出统一格式 |
| 想检查字幕是否完整 | "帮我看看字幕有没有缺行" | 执行字段校验并报告异常 |

---

## 三、标准流程

### 3.1 前置条件

| 条件项 | 要求 | 检查方式 |
|--------|------|----------|
| 输入文件格式 | .srt / .vtt / .json | 文件扩展名确认 |
| 文件命名 | 建议使用 `video_id.srt` 或 `video_id.vtt` 格式 | 目视检查 |
| 文件位置 | 与 Skill 工作目录一致 | `ls` 命令确认 |
| 文件完整性 | 文件大小 > 0 字节 | `wc -c` 命令确认 |
| 编码格式 | UTF-8（无 BOM） | `file` 命令确认 |

### 3.2 执行步骤

#### 步骤 1：环境确认

```bash
# 检查当前目录文件列表
ls -la

# 确认文件编码
file *.srt *.vtt *.json 2>/dev/null
```

#### 步骤 2：单样本试运行

选取一个代表性文件执行解析：

```bash
# 示例：解析单个 SRT 文件
python -m youtube_transcript_api_sharp parse sample.srt --format json
```

**预期输出示例：**

```json
{
  "source_file": "sample.srt",
  "parse_status": "success",
  "entries": [
    {
      "index": 1,
      "start_time": "00:00:01,000",
      "end_time": "00:00:04,500",
      "text": "欢迎收看本期视频",
      "duration_ms": 3500
    }
  ],
  "total_entries": 1
}
```

**核对要点：**
- 时间戳格式是否符合预期（SRT 用逗号，VTT 用句点）
- 文本内容是否完整保留
- 序号是否连续

#### 步骤 3：批量执行

确认单样本无误后，对全量文件执行：

```bash
# 批量解析当前目录所有字幕文件
python -m youtube_transcript_api_sharp batch --input-dir ./ --output-dir ./output --format json
```

**执行前备份：**

```bash
# 创建备份目录
mkdir -p ./backup_$(date +%Y%m%d)
cp *.srt *.vtt *.json ./backup_$(date +%Y%m%d)/ 2>/dev/null
```

#### 步骤 4：结果校验

抽查输出条目，核对以下字段：

| 校验项 | 校验方法 | 通过标准 |
|--------|----------|----------|
| 时间戳连续性 | 检查相邻条目时间是否重叠 | 无重叠且无大于 5 秒的间隙 |
| 文本完整性 | 随机抽取 5 条与源文件比对 | 文本一致率 100% |
| 序号连续性 | 检查 index 字段 | 从 1 开始，无跳号 |
| 文件完整性 | 比较输入输出文件数 | 输出文件数 = 输入文件数 |

### 3.3 输出规范

#### 标准输出格式（JSON）

```json
{
  "schema_version": "1.0",
  "generated_at": "2024-01-15T10:30:00Z",
  "source": {
    "file_name": "video_123.srt",
    "format": "srt",
    "file_size_bytes": 2048
  },
  "parse_summary": {
    "total_entries": 42,
    "total_duration_ms": 252000,
    "parse_errors": 0
  },
  "entries": [
    {
      "index": 1,
      "start_time": "00:00:01,000",
      "end_time": "00:00:04,500",
      "text": "字幕文本内容",
      "duration_ms": 3500
    }
  ]
}
```

#### 字段说明

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| schema_version | string | 是 | 输出格式版本号 |
| generated_at | string | 是 | 生成时间（ISO 8601） |
| source.file_name | string | 是 | 源文件名 |
| source.format | string | 是 | 源格式（srt/vtt/json） |
| entries[].index | integer | 是 | 字幕序号 |
| entries[].start_time | string | 是 | 开始时间（HH:MM:SS,mmm） |
| entries[].end_time | string | 是 | 结束时间（HH:MM:SS,mmm） |
| entries[].text | string | 是 | 字幕文本 |
| entries[].duration_ms | integer | 否 | 持续时间（毫秒） |

---

## 四、置信度门控

### 4.1 信息不足时的处理

当遇到以下情况时，输出 `[需核实:字段名]` 占位符，不进行猜测：

| 场景 | 处理方式 | 示例 |
|------|----------|------|
| 时间戳格式异常 | 保留原始值，标记需核实 | `"start_time": "[需核实:start_time]"` |
| 文本内容不完整 | 标记缺失部分 | `"text": "前半句[需核实:后半句缺失]"` |
| 序号不连续 | 标记跳号位置 | `"index": "[需核实:index_跳号]"` |
| 文件编码异常 | 停止解析，提示用户 | 不输出结果，返回错误码 |

### 4.2 置信度分级

| 置信度等级 | 判定标准 | 输出行为 |
|-----------|----------|----------|
| 高（≥95%） | 所有字段校验通过 | 正常输出 |
| 中（80-94%） | 个别字段需人工确认 | 输出并附带警告信息 |
| 低（<80%） | 关键字段缺失或异常 | 输出占位符并建议人工检查 |

---

## 五、错误码体系

### 5.1 常见错误码

| 错误码 | 错误描述 | 提示话术 | 修正步骤 |
|--------|----------|----------|----------|
| E001 | 文件不存在 | "未找到指定文件，请检查路径" | 1. 确认文件路径 2. 检查文件名拼写 3. 确认文件权限 |
| E002 | 文件格式不支持 | "仅支持 .srt / .vtt / .json 格式" | 1. 转换文件格式 2. 确认扩展名正确 |
| E003 | 文件编码异常 | "文件编码不是 UTF-8，可能包含乱码" | 1. 使用 `iconv` 转换编码 2. 重新导出字幕 |
| E004 | 时间戳解析失败 | "时间戳格式不符合规范" | 1. 检查时间戳分隔符 2. 确认毫秒位为 3 位数字 |
| E005 | 字幕内容为空 | "文件中未检测到有效字幕内容" | 1. 确认文件非空 2. 检查文件是否被截断 |
| E006 | 批量处理中断 | "批量处理在第 N 个文件处中断" | 1. 查看错误日志 2. 跳过问题文件重新执行 |

### 5.2 错误处理流程

```
遇到错误
    ↓
记录错误码和上下文
    ↓
输出错误提示（含修正建议）
    ↓
询问用户是否继续或终止
    ↓
根据用户选择执行
```

---

## 六、FAQ 反模式

### 6.1 常见坑与反模式对照

| 编号 | 常见坑 | 反模式（错误做法） | 正确做法 |
|------|--------|-------------------|----------|
| F1 | 时间戳格式混淆 | 将 SRT 的逗号毫秒分隔直接用于 VTT | 根据源格式自动适配分隔符（SRT 用逗号，VTT 用句点） |
| F2 | 批量处理前不试运行 | 直接对全量文件执行，导致错误扩散 | 先单样本验证，再批量执行 |
| F3 | 忽略原始文件备份 | 解析后覆盖源文件 | 执行前创建带时间戳的备份目录 |
| F4 | 输出格式不统一 | 不同文件输出不同结构 | 统一使用 schema_version 1.0 标准格式 |
| F5 | 不校验直接使用 | 解析结果未经校验直接用于下游 | 按 3.2 步骤 4 执行字段校验 |

### 6.2 反模式示例

**反模式 F1 示例：**

```bash
# 错误：将 SRT 时间戳直接用于 VTT
00:00:01,000 --> 00:00:04,500  # SRT 格式

# 正确：VTT 使用句点
00:00:01.000 --> 00:00:04.500  # VTT 格式
```

**反模式 F2 示例：**

```bash
# 错误：直接批量执行
python -m youtube_transcript_api_sharp batch --input-dir ./ --output-dir ./output

# 正确：先单样本验证
python -m youtube_transcript_api_sharp parse sample.srt --format json
# 确认无误后再批量执行
```

---

## 七、渐进式披露

### 7.1 速查卡（新手快速上手）

```
1. 放文件 → 确认 .srt/.vtt/.json 格式
2. 试运行 → 解析单个文件，检查输出
3. 批量跑 → 全量执行，先备份
4. 查结果 → 抽查 5 条，核对时间戳和文本
```

### 7.2 分层次阅读路径

#### 新手路径（首次使用）

1. 阅读「一、能力边界」了解能做什么
2. 按「三、标准流程」步骤 1-2 执行单样本
3. 确认输出格式符合预期
4. 按步骤 3-4 批量执行并校验

#### 进阶路径（熟练用户）

1. 阅读「四、置信度门控」了解异常处理
2. 阅读「五、错误码体系」掌握错误排查
3. 阅读「六、FAQ 反模式」避免常见错误
4. 自定义输出格式（修改 schema 字段）

#### 专家路径（深度定制）

1. 扩展支持更多字幕格式
2. 自定义字段校验规则
3. 集成到自动化流水线
4. 开发自定义输出模板

---

## 八、参数速查表

### 8.1 CLI 参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `--input` | string | 是 | 无 | 输入文件路径 |
| `--format` | string | 否 | json | 输出格式（json/srt/vtt） |
| `--output-dir` | string | 否 | ./output | 输出目录 |
| `--batch` | flag | 否 | false | 批量模式 |
| `--selftest` | flag | 否 | false | 自检模式 |
| `--version` | flag | 否 | false | 显示版本号 |

### 8.2 边界值

| 参数 | 最小值 | 最大值 | 说明 |
|------|--------|--------|------|
| 文件大小 | 1 字节 | 10 MB | 超过 10MB 建议分片处理 |
| 字幕条目数 | 1 条 | 10000 条 | 超过 10000 条建议分批 |
| 单条文本长度 | 1 字符 | 500 字符 | 超过 500 字符可能截断 |
| 时间戳精度 | 毫秒（3 位） | 毫秒（3 位） | 不支持微秒精度 |

---

## 九、用户协议

<!-- user-agreement-injected -->

**使用本 Skill 即表示您同意以下条款：**

1. **责任承担**：使用者自行承担使用本 Skill 的全部责任。因使用本 Skill 产生的任何直接或间接损失，Skill 作者及贡献者不承担任何责任。

2. **合法使用**：使用者承诺仅将本 Skill 用于合法目的，遵守所在地法律法规及 YouTube 服务条款。

3. **禁止反向工程**：使用者不得对本 Skill 进行反向工程、反编译、反汇编或试图提取源代码（适用法律允许的除外）。

4. **无担保**：本 Skill 按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权保证。

5. **免责声明**：本 Skill 由 AI 辅助生成，仅供学习参考。使用前请阅读相关文档，自行评估适用性。

---

## 十、许可证（License）

<!-- professional-license-embedded -->

**MIT License**

版权所有 (c) 2024 skill-forge-lab

特此免费授予任何获得本软件及相关文档文件（以下简称"软件"）副本的人士处理软件的权限，包括不受限制地使用、复制、修改、合并、发布、分发、再许可和/或销售软件副本的权利，并允许向其提供软件的人士这样做，但须满足以下条件：

上述版权声明和本许可声明应包含在软件的所有副本或实质性部分中。

本软件按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权保证。在任何情况下，作者或版权持有人均不对因使用本软件或与本软件有关的任何索赔、损害或其他责任负责，无论是在合同、侵权或其他方面。

---

*本 Skill 文档由 AI 辅助生成，仅供参考。使用前请阅读相关文档。*
