---
<!-- © 2026 SkillForge Lab. All rights reserved. -->
slug: scientific-agent-skills
name: scientific-agent-skills
displayName: 科研数据 结构化解析 智能转换
description: 将科研数据、文件或URL转化为结构化结果，辅助科学分析。
version: 1.0.3
rules_version: cpr-20260820-n601
license: MIT
source_project: original
source_url: https://github.com/bluebigman/skill-factory-originals/tree/main/scientific-agent-skills
copyright_holder: 原创作者（自持版权）
ai_generated: true
ai_tools: ["DeepSeek"]
disclaimer: 本Skill由AI辅助生成，提供使用指导和最佳实践。使用前请阅读相关文档。
author: DataForge Lab
agent_created: true
trigger_words: ["scientific agent skills", "科研数据处理", "科学数据解析", "结构化输出", "数据转换", "实验数据整理", "文献信息抽取"]
---

> ⚠️ **本内容仅供一般信息参考，不构成法律、财务、税务、投资或医疗建议。**
> 涉及合同签署、报税、投资、诊疗等专业决策时，请务必咨询持证专业人士，并由使用者自行承担决策后果。
<!-- professional-disclaimer-injected -->


> 本内容由 AI 生成，仅供学习参考
<!-- ai-generated-notice -->

# 科研数据 结构化解析 智能转换（scientific-agent-skills）

## 一、能力边界速查卡

### 1.1 能做什么

| 能力项 | 说明 | 输入示例 | 输出示例 |
|--------|------|----------|----------|
| 文件解析 | 读取常见科研数据文件（CSV、TXT、JSON、Excel） | `experiment_log.csv` | 结构化表格 + 字段类型标注 |
| URL 抓取 | 从公开网页/数据仓库提取表格或数值 | `https://example.org/data.html` | 规范化数据行 + 来源元数据 |
| 数据清洗 | 去除空行、重复项、异常值标记 | 含 `NA`、`null`、空格的原始表 | 清洗报告 + 清洗后数据 |
| 字段映射 | 将非标准列名映射为统一 schema | `col1`、`col2` → `timestamp`、`value` | 映射字典 + 转换后数据 |
| 汇总统计 | 生成均值、中位数、标准差等描述性统计 | 数值型数据列 | 统计摘要表 |

### 1.2 不能做什么

- **不能** 处理二进制专有格式（如 `.sav`、`.mat` 需先转换为文本格式）
- **不能** 访问需要登录或付费墙后的数据源
- **不能** 执行统计分析或建模（仅做结构化整理，不做推断）
- **不能** 保证数据准确性——只负责格式转换，不校验源数据真伪
- **不能** 处理超过 50MB 的单个文件（内存限制）

### 1.3 适用对象

- 科研助理：需要快速整理实验记录
- 数据分析师：需要统一多来源数据格式
- 文献综述者：需要从 PDF/HTML 中抽取表格数据
- 实验室管理员：需要汇总仪器导出文件

---

## 二、触发方式与场景映射

### 2.1 触发词

直接使用以下任一短语即可激活：

- `scientific agent skills`
- `科研数据处理`
- `科学数据解析`
- `结构化输出`
- `数据转换`
- `实验数据整理`
- `文献信息抽取`

### 2.2 场景映射表

| 你说的话（大白话） | 实际触发动作 | 预期结果 |
|-------------------|-------------|----------|
| "帮我把这个 CSV 整理一下" | 文件解析 + 数据清洗 | 干净的结构化表格 |
| "这个网页里的数据能提取吗" | URL 抓取 + 字段映射 | 规范化数据行 |
| "我的 Excel 里列名太乱了" | 字段映射 + 重命名 | 统一 schema 的数据 |
| "跑一下这个文件夹里所有文件" | 批量处理 | 每个文件对应一个输出 |
| "看看这批数据有没有问题" | 数据质量检查 | 异常值报告 |

---

## 三、标准执行流程

### 3.1 前置条件

| 条件 | 要求 | 检查方式 |
|------|------|----------|
| 输入文件 | 文本格式（CSV/TXT/JSON/Excel），≤50MB | 文件大小属性 |
| 数据内容 | 不含个人隐私/商业机密/受法律保护数据 | 使用者自行确认 |
| 网络访问 | 仅 URL 抓取时需要 | `ping` 或浏览器测试 |
| 输出目录 | 有写权限的文件夹 | `os.access(path, os.W_OK)` |

### 3.2 执行步骤

**Step 1：输入确认**

- 列出所有待处理文件路径或 URL
- 确认每个文件的格式类型
- 询问使用者是否需要批量模式

**Step 2：单文件试运行**

- 选取第一个文件执行完整流程
- 展示输出预览（前 5 行 + 字段列表）
- 等待使用者确认格式是否符合预期

**Step 3：格式调整（如需要）**

- 根据反馈调整字段映射规则
- 修改清洗参数（如缺失值填充策略）
- 重新执行试运行

**Step 4：批量执行**

- 按确认后的参数处理全部文件
- 每个文件独立生成输出
- 输出文件命名规则：`原文件名_processed.ext`

**Step 5：结果抽查**

- 随机选取 20% 输出文件进行人工核对
- 检查字段完整性、行数一致性
- 生成处理报告（含每个文件的处理状态）

### 3.3 输出规范

输出文件统一为 UTF-8 编码的 CSV 或 JSON，包含：

```json
{
  "schema_version": "1.0",
  "source_file": "experiment_log.csv",
  "processed_at": "2026-08-20T14:30:00Z",
  "row_count": 152,
  "columns": [
    {"name": "timestamp", "type": "datetime", "null_count": 0},
    {"name": "temperature", "type": "float", "null_count": 3}
  ],
  "data_preview": [...]
}
```

---

## 四、置信度门控机制

### 4.1 信息不足时的处理

当遇到以下情况，**不得**编造数据，必须输出占位符：

| 场景 | 占位符 | 示例 |
|------|--------|------|
| 字段含义不明确 | `[需核实:字段名]` | `[需核实:col3]` |
| 数值单位未知 | `[需核实:单位]` | `[需核实:单位]` |
| 时间格式不确定 | `[需核实:时间格式]` | `[需核实:时间格式]` |
| 数据来源不可验证 | `[需核实:来源]` | `[需核实:来源]` |

### 4.2 置信度评分

每次输出附带置信度评分（0-100）：

- **90-100**：所有字段类型明确，无缺失值，来源可验证
- **70-89**：存在少量缺失值或字段名模糊，但整体结构清晰
- **50-69**：多个字段需要人工确认，或数据来源不可靠
- **<50**：建议放弃该数据源，重新获取

---

## 五、错误码体系

| 错误码 | 含义 | 提示话术 | 修正步骤 |
|--------|------|----------|----------|
| `E001` | 文件不存在 | "未找到指定文件，请检查路径" | 1. 确认路径拼写 2. 检查文件是否被移动 |
| `E002` | 格式不支持 | "该文件格式不在支持列表中" | 1. 转换为 CSV/TXT 2. 联系管理员扩展格式 |
| `E003` | 文件过大 | "文件超过 50MB 限制" | 1. 分割文件 2. 抽样处理 |
| `E004` | URL 无法访问 | "目标网址返回 404 或超时" | 1. 检查 URL 拼写 2. 确认网络连接 |
| `E005` | 编码错误 | "文件编码无法识别，请指定" | 1. 尝试 UTF-8 2. 尝试 GBK 3. 尝试 Latin-1 |
| `E006` | 字段映射失败 | "存在无法映射的列，请提供映射规则" | 1. 查看未映射列名 2. 补充映射字典 |
| `E007` | 输出目录无权限 | "无法写入输出目录" | 1. 更换目录 2. 修改目录权限 |

---

## 六、FAQ 与反模式对照

### 6.1 常见坑

| 坑 | 反模式（错误做法） | 正确做法 |
|----|-------------------|----------|
| 数据源污染 | 直接信任所有输入数据 | 先运行数据质量检查，标记可疑值 |
| 过度清洗 | 删除所有含缺失值的行 | 保留行，用 `NA` 标记，统计缺失率 |
| 格式误判 | 假设所有 CSV 都是逗号分隔 | 检查分隔符（逗号/分号/制表符） |
| 批量处理失控 | 不试运行直接跑全部文件 | 先跑 1 个文件，确认后再批量 |
| 忽略元数据 | 丢弃原始文件中的注释行 | 将注释提取到 `metadata` 字段 |

### 6.2 反模式对照表

| 反模式 | 问题 | 替代方案 |
|--------|------|----------|
| "把所有数据都转成一种格式" | 丢失原始精度 | 保留原始格式副本，输出转换版本 |
| "这个数据肯定没问题" | 未经验证 | 抽查 20% 数据，交叉验证 |
| "直接跑吧，不用看" | 错误累积 | 每步输出预览，人工确认 |
| "把所有列都保留" | 冗余字段干扰分析 | 按需选择列，记录丢弃列名 |

---

## 七、渐进式披露路径

### 7.1 新手路径（5 分钟上手）

1. 阅读「能力边界速查卡」了解基本功能
2. 准备一个 CSV 文件
3. 触发 `科研数据处理` + 文件路径
4. 查看输出预览，确认格式
5. 使用默认参数完成处理

### 7.2 进阶路径（深度使用）

1. 学习「标准执行流程」中的参数配置
2. 自定义字段映射字典
3. 使用批量模式处理多文件
4. 解读置信度评分和数据质量报告
5. 结合外部工具（如 pandas）进一步分析

### 7.3 专家路径（定制化）

1. 修改 schema 定义以匹配特定领域标准
2. 编写自定义清洗规则（如单位换算）
3. 集成到自动化工作流中
4. 扩展支持自定义文件格式

---

## 八、参数配置参考

### 8.1 常用参数表

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `delimiter` | string | `auto` | 文件分隔符（逗号/分号/制表符） |
| `encoding` | string | `utf-8` | 文件编码 |
| `header_row` | int | `0` | 表头所在行索引 |
| `null_values` | list | `["", "NA", "null", "NaN"]` | 视为缺失值的字符串 |
| `date_format` | string | `auto` | 日期时间解析格式 |
| `output_format` | string | `csv` | 输出格式（csv/json） |
| `batch_mode` | bool | `false` | 是否批量处理 |
| `sample_size` | int | `5` | 预览行数 |

### 8.2 边界值说明

- **文件大小**：硬上限 50MB，超过返回 `E003`
- **列数限制**：最多支持 200 列，超出部分截断并警告
- **行数限制**：单文件最多 100 万行，超出建议分片
- **URL 超时**：默认 10 秒，可配置 `timeout` 参数
- **并发限制**：批量模式最多同时处理 5 个文件

---

## 九、用户协议

<!-- user-agreement-injected -->

**使用本 Skill 即表示您同意以下条款：**

1. **责任承担**：使用者自行承担因使用本 Skill 产生的全部责任。本 Skill 仅提供数据处理功能，不构成任何专业建议。因数据错误、遗漏或不当使用导致的任何损失，本 Skill 开发者不承担任何责任。

2. **数据安全**：使用者应确保输入数据不包含个人隐私信息、商业机密或受法律保护的数据。本 Skill 不提供数据加密传输功能，请勿处理敏感数据。

3. **禁止反向工程**：使用者不得对本 Skill 进行反向工程、反编译、破解或试图提取底层算法。不得移除、修改或遮蔽文档中的任何版权声明。

4. **合规使用**：使用者应遵守所在国家/地区的法律法规，不得将本 Skill 用于非法用途，包括但不限于数据窃取、侵犯他人知识产权等行为。

5. **免责声明**：本 Skill 按"原样"提供，不附带任何明示或暗示的保证，包括但不限于适销性、特定用途适用性和非侵权保证。

---

## 十、许可证（License）

<!-- professional-license-embedded -->

**MIT License**

版权所有 (c) 2026 原创作者（自持版权）

特此免费授予任何获得本软件及相关文档文件（"软件"）副本的人，不受限制地处理本软件，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或出售本软件的副本，并允许向其提供本软件的人这样做，但须满足以下条件：

上述版权声明和本许可声明应包含在本软件的所有副本或主要部分中。

本软件按"原样"提供，不附带任何明示或暗示的保证，包括但不限于适销性、特定用途适用性和非侵权保证。在任何情况下，作者或版权持有人均不对任何索赔、损害或其他责任负责，无论是在合同诉讼、侵权或其他方面，由本软件或本软件的使用或其他交易引起、产生或与之相关。

---

## 十一、版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 1.0.0 | 2026-08-20 | 初始版本，包含核心解析、清洗、映射功能 |

---

*本 Skill 由 AI 辅助生成，仅供参考。使用前请阅读相关文档。*
