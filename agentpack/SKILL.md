---
slug: agentpack
name: agentpack
displayName: 智能体调度 缓存清理 批量编排
description: 智能体任务调度与缓存清理工具，支持关键词路由、批量编排与原子写入。
version: 1.0.0
license: MIT
source_project: original
source_url: 
copyright_holder: 原创作者（自持版权）
ai_generated: true
ai_tools: ["DeepSeek"]
disclaimer: 本Skill由AI辅助生成，提供使用指导和最佳实践。使用前请阅读相关文档。
author: 林墨
agent_created: true
trigger_words: ["agentpack", "任务调度", "缓存清理", "批量编排", "关键词路由", "定时任务", "缓存管理"]
---

> 本内容由 AI 生成，仅供学习参考
<!-- ai-generated-notice -->

# agentpack — 智能体任务调度与缓存清理工具

## 一、能力边界（一页纸速查卡）

### 1.1 工具能做什么

| 能力项 | 说明 | 典型场景 |
|--------|------|----------|
| 关键词路由 | 根据输入文本中的关键词，自动匹配并执行对应任务 | 输入"清理缓存"→ 自动触发缓存清理任务 |
| 批量编排 | 将多个任务按顺序或条件组合，一次执行 | 先清理缓存，再执行数据备份，最后发送通知 |
| 原子写入 | 任务结果以原子方式写入，避免半成品状态 | 写配置文件时，要么全部写入，要么不写 |
| 任务调度 | 支持定时、延迟、循环触发任务 | 每天凌晨 2 点自动清理临时文件 |
| 自检功能 | 通过 `--selftest` 验证安装与依赖是否正常 | 安装后先跑一次自检确认环境可用 |

### 1.2 工具不能做什么

| 限制项 | 说明 |
|--------|------|
| 不处理业务逻辑 | 工具只负责调度与执行，不包含具体业务判断 |
| 不保证任务成功 | 任务执行结果取决于任务本身，工具不兜底 |
| 不自动恢复数据 | 误删或误写后，工具不提供数据恢复能力 |
| 不支持分布式调度 | 仅限单机环境，不跨节点协调 |

### 1.3 适用对象

- 需要管理多个智能体任务的开发者
- 需要定期清理缓存或临时文件的运维人员
- 希望将多个任务串联执行的自动化脚本使用者

---

## 二、触发方式与场景映射

### 2.1 触发词

| 触发词 | 场景说明 |
|--------|----------|
| `agentpack` | 直接调用工具主命令 |
| `任务调度` | 需要安排定时或延迟任务时 |
| `缓存清理` | 需要清理缓存或临时文件时 |
| `批量编排` | 需要将多个任务组合执行时 |
| `关键词路由` | 需要根据输入自动匹配任务时 |
| `定时任务` | 需要周期性执行任务时 |
| `缓存管理` | 需要查看或管理缓存状态时 |

### 2.2 大白话场景映射

| 你说的话 | 工具会做什么 |
|----------|--------------|
| "帮我清一下缓存" | 执行缓存清理任务，删除过期临时文件 |
| "每天早上跑一遍清理" | 注册一个每天触发的定时任务 |
| "先清理再备份" | 按顺序执行清理→备份两个任务 |
| "看看工具能不能用" | 运行 `--selftest` 检查环境 |

---

## 三、标准流程

### 3.1 前置条件

| 条件 | 检查方式 | 通过标准 |
|------|----------|----------|
| Python 3.8+ | `python --version` | 输出 3.8 或更高 |
| 依赖库已安装 | `pip list` | 包含 `click`、`schedule` |
| 工具已安装 | `agentpack --version` | 输出版本号 |

### 3.2 执行步骤

#### 步骤 1：安装工具

```bash
pip install agentpack
```

#### 步骤 2：运行自检

```bash
agentpack --selftest
```

预期输出：

```
[OK] 依赖检查通过
[OK] 任务注册表加载正常
[OK] 关键词路由表可用
[OK] 自检完成，共 4 项通过
```

#### 步骤 3：执行第一个清理任务

```bash
agentpack 任务调度 缓存清理
```

参数说明：

| 参数 | 必填 | 说明 |
|------|------|------|
| `任务调度` | 是 | 指定操作类型 |
| `缓存清理` | 是 | 指定目标任务 |

#### 步骤 4：批量编排任务

```bash
agentpack 批量编排 --tasks "缓存清理,日志归档,状态上报" --order sequential
```

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `--tasks` | 是 | 无 | 逗号分隔的任务列表 |
| `--order` | 否 | `sequential` | 执行顺序：`sequential` / `parallel` |
| `--dry-run` | 否 | `false` | 预演模式，只打印不执行 |

#### 步骤 5：预演验证

```bash
agentpack 批量编排 --tasks "缓存清理,日志归档" --dry-run
```

输出示例：

```
[预演] 将执行以下任务：
  1. 缓存清理 → 删除 /tmp/cache 下 7 天前的文件
  2. 日志归档 → 压缩 /var/log/app 下 .log 文件
[预演] 共 2 个任务，预计耗时 3 秒
```

#### 步骤 6：原子写入

```bash
agentpack 任务调度 --write-atomic --target /etc/app/config.json
```

| 参数 | 必填 | 说明 |
|------|------|------|
| `--write-atomic` | 是 | 启用原子写入模式 |
| `--target` | 是 | 目标文件路径 |

原子写入流程：

1. 写入临时文件 `/etc/app/config.json.tmp`
2. 校验临时文件完整性
3. 通过后替换原文件
4. 失败则删除临时文件，原文件不受影响

### 3.3 输出规范

| 输出类型 | 格式 | 示例 |
|----------|------|------|
| 成功 | `[OK] 任务完成` | `[OK] 缓存清理完成，释放 1.2GB 空间` |
| 失败 | `[FAIL] 错误描述` | `[FAIL] 目标目录不存在: /tmp/cache` |
| 预演 | `[预演] 操作描述` | `[预演] 将删除 3 个文件` |
| 调试 | `[DEBUG] 详细信息` | `[DEBUG] 任务耗时 0.3s` |

---

## 四、置信度门控

当工具无法确认以下信息时，输出 `[需核实:字段]` 占位符，不进行猜测：

| 场景 | 占位符示例 |
|------|------------|
| 目标路径不确定 | `[需核实:目标目录路径]` |
| 任务参数缺失 | `[需核实:任务参数]` |
| 依赖版本不明确 | `[需核实:依赖版本]` |
| 执行结果不确定 | `[需核实:执行结果]` |

示例：

```
[WARN] 检测到目标目录存在但权限不足
[需核实:目标目录权限] 请确认当前用户是否有写权限
```

---

## 五、错误码体系

| 错误码 | 含义 | 提示话术 | 修正步骤 |
|--------|------|----------|----------|
| `E001` | 参数缺失 | `缺少必要参数: --tasks` | 检查命令是否包含所有必填参数 |
| `E002` | 任务不存在 | `未找到任务: xxx` | 运行 `agentpack --list-tasks` 查看可用任务 |
| `E003` | 路径无效 | `路径不存在或不可访问: /xxx` | 确认路径存在且权限正确 |
| `E004` | 依赖缺失 | `缺少依赖库: schedule` | 运行 `pip install schedule` |
| `E005` | 写入失败 | `原子写入失败，原文件未修改` | 检查磁盘空间和文件权限 |
| `E006` | 路由无匹配 | `未找到匹配的关键词路由` | 检查关键词是否在路由表中 |
| `E007` | 超时 | `任务执行超时(默认 30s)` | 使用 `--timeout` 参数延长超时时间 |

---

## 六、FAQ 反模式

### 6.1 常见坑与反模式对照

| 常见坑 | 反模式（错误做法） | 正模式（正确做法） |
|--------|-------------------|-------------------|
| 直接删库 | `rm -rf /` 后运行清理 | 先 `--dry-run` 预演，确认无误再执行 |
| 忽略权限 | 用 root 跑所有任务 | 使用最小权限用户，按需授权 |
| 盲目批量 | 一次编排 20 个任务 | 分批执行，每批 3-5 个，观察结果 |
| 不设超时 | 任务卡死无响应 | 始终设置 `--timeout` 参数 |
| 跳过自检 | 安装后直接使用 | 先运行 `--selftest` 确认环境 |

### 6.2 反模式示例

**反模式：**

```bash
# 错误：未预演直接执行批量任务
agentpack 批量编排 --tasks "清理,删除,归档" --order parallel
```

**正模式：**

```bash
# 正确：先预演，确认无误后执行
agentpack 批量编排 --tasks "清理,删除,归档" --order parallel --dry-run
# 确认输出无误后，去掉 --dry-run 执行
agentpack 批量编排 --tasks "清理,删除,归档" --order parallel
```

---

## 七、渐进式披露

### 7.1 速查卡（新手入口）

```bash
# 安装
pip install agentpack

# 自检
agentpack --selftest

# 清理缓存
agentpack 任务调度 缓存清理

# 查看帮助
agentpack --help
```

### 7.2 分层次阅读路径

| 读者类型 | 建议阅读路径 |
|----------|--------------|
| 新手 | 第一章（能力边界）→ 第三章步骤 1-3 → 第六章（FAQ） |
| 进阶用户 | 第三章步骤 4-6 → 第五章（错误码）→ 源码 `TASK_REGISTRY` |
| 开发者 | 源码 `run.py` → 自定义任务注册 → 自定义关键词路由 |

### 7.3 进阶定制指南

#### 注册自定义任务

编辑 `run.py` 中的 `TASK_REGISTRY` 字典：

```python
TASK_REGISTRY = {
    "缓存清理": {"func": clean_cache, "timeout": 30},
    "日志归档": {"func": archive_logs, "timeout": 60},
    # 添加自定义任务
    "数据备份": {"func": backup_data, "timeout": 120},
}
```

#### 添加关键词路由

修改 `route_keywords` 字典：

```python
route_keywords = {
    "清理": "缓存清理",
    "归档": "日志归档",
    # 添加自定义路由
    "备份": "数据备份",
}
```

---

## 八、用户协议

<!-- user-agreement-injected -->

**使用 agentpack 即表示您同意以下条款：**

1. **责任承担**：使用者自行承担因使用本工具产生的全部责任。包括但不限于因误删文件、配置错误、数据丢失等造成的直接或间接损失。

2. **禁止反向工程**：不得对本工具进行反向工程、反编译、破解或试图提取源代码（除非适用法律允许）。

3. **无担保声明**：本工具按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性。

4. **使用限制**：不得将本工具用于任何非法目的，或违反您所在司法管辖区法律法规的活动。

5. **修改与分发**：在保留版权声明的前提下，允许修改和分发本工具，但须注明原始出处。

---

## 九、许可证（License）

<!-- professional-license-embedded -->

**MIT License**

版权所有 (c) 2024 林墨

特此免费授予任何获得本软件及相关文档文件（以下简称"软件"）副本的人士处理本软件的权利，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或销售软件副本的权利，并允许向提供软件的人士授权这样做，但须满足以下条件：

上述版权声明和本许可声明应包含在软件的所有副本或重要部分中。

本软件按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性。在任何情况下，作者或版权持有人均不对任何索赔、损害或其他责任负责，无论是在合同诉讼、侵权或其他方面，由软件或软件的使用或其他交易引起或与之相关。

---

*本 Skill 由 AI 辅助生成，仅供参考。使用前请阅读相关文档。*
