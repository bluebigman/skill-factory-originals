---
<!-- © 2026 SkillForge Lab. All rights reserved. -->
slug: agentpack
name: agentpack
displayName: 任务编排 缓存清理 智能调度
description: 智能体任务调度与缓存清理工具，支持关键词路由、批量任务编排、安全预演与原子化写入。
version: 2.0.5
rules_version: cpr-20260817-n526
license: MIT
source_project: original
source_url: https://github.com/bluebigman/skill-factory-originals/tree/main/agentpack
copyright_holder: 原创作者（自持版权）
ai_generated: true
ai_tools: ["DeepSeek"]
disclaimer: 本Skill由AI辅助生成，提供使用指导和最佳实践。使用前请阅读相关文档。
author: Lin Chen
agent_created: true
trigger_words: ["agentpack", "任务调度", "缓存清理", "批量编排", "关键词路由", "安全预演", "原子写入"]

> 本内容由 AI 生成，仅供学习参考
<!-- ai-generated-notice -->

---

> ⚠️ **本内容仅供一般信息参考，不构成法律、财务、税务、投资或医疗建议。**
> 涉及合同签署、报税、投资、诊疗等专业决策时，请务必咨询持证专业人士，并由使用者自行承担决策后果。
<!-- professional-disclaimer-injected -->


# agentpack — 智能体任务调度与缓存清理工具

## 一、能力边界（一页纸速查卡）

### 1.1 能做什么

| 能力项 | 说明 | 典型场景 |
|--------|------|----------|
| 关键词路由 | 根据输入文本中的关键词，将任务分发到对应的处理函数 | 用户说"清理缓存"→ 自动路由到 cache_clean 函数 |
| 批量任务编排 | 支持将多个子任务按顺序或依赖关系组合执行 | 先备份配置 → 再清理缓存 → 最后生成报告 |
| 安全预演 | 在执行写操作前，先模拟执行并输出将要变更的内容 | 删除文件前，列出将被删除的文件清单 |
| 原子化写入 | 确保写入操作要么全部成功，要么全部失败，不留中间状态 | 批量更新配置文件时，防止写入一半崩溃导致配置损坏 |
| 自检与版本查询 | 提供 `--selftest` 和 `--version` 两个 CLI 接口 | 验证安装是否成功、查看当前版本号 |

### 1.2 不能做什么

| 限制项 | 说明 |
|--------|------|
| 不支持分布式调度 | 仅限单机环境，不提供跨节点任务分发 |
| 不包含图形界面 | 纯命令行工具，无 Web UI 或桌面 GUI |
| 不提供持久化队列 | 任务队列仅存于内存，进程重启后队列丢失 |
| 不处理外部服务依赖 | 不负责启动/停止数据库、消息队列等外部服务 |
| 不做数据备份 | 清理缓存前不会自动备份，需用户自行确认 |

### 1.3 适用对象

- **AI 智能体开发者**：需要为 Agent 增加任务调度与缓存管理能力
- **运维工程师**：需要批量清理临时文件、缓存目录的自动化脚本
- **Python 脚本爱好者**：需要一个轻量级的任务编排工具

---

## 二、触发方式

### 2.1 触发词

| 触发词 | 同义场景词 | 触发场景示例 |
|--------|-----------|-------------|
| agentpack | 任务调度工具、缓存清理器 | 用户输入"用 agentpack 清理一下缓存" |
| 任务调度 | 任务编排、批量执行 | 用户输入"帮我编排一个清理任务" |
| 缓存清理 | 临时文件清理、垃圾回收 | 用户输入"清理 /tmp 下的临时文件" |
| 批量编排 | 批量处理、任务组合 | 用户输入"把备份和清理组合成一个任务" |
| 安全预演 | 模拟执行、预检查 | 用户输入"先看看会删哪些文件" |
| 原子写入 | 事务性写入、全有或全无 | 用户输入"确保配置更新不会写一半" |

### 2.2 大白话场景映射表

| 用户说（大白话） | 实际触发能力 | 执行动作 |
|-----------------|-------------|---------|
| "帮我清一下电脑垃圾" | 缓存清理 | 扫描缓存目录，列出可清理文件，确认后删除 |
| "把这几件事按顺序跑一遍" | 批量任务编排 | 按依赖顺序依次执行子任务 |
| "先别删，让我看看会删什么" | 安全预演 | 执行 dry-run 模式，输出变更清单 |
| "更新配置别搞坏了" | 原子化写入 | 写入临时文件，校验后原子替换 |
| "这工具能用吗？" | 自检 | 运行 `--selftest` 验证功能完整性 |

---

## 三、标准流程

### 3.1 前置条件

| 条件 | 要求 | 验证方式 |
|------|------|---------|
| Python 版本 | ≥ 3.8 | `python --version` |
| 操作系统 | Linux / macOS / Windows（需支持 `os.replace`） | 无需验证，代码自动检测 |
| 磁盘空间 | 至少 10MB 可用空间（用于临时文件） | `df -h`（Linux/macOS） |
| 权限 | 对目标缓存目录有读写权限 | 运行 `--selftest` 自动检测 |

### 3.2 执行步骤

#### 步骤 1：安装与验证

```bash
# 将 run.py 保存到项目目录
# 赋予执行权限（可选）
chmod +x run.py

# 验证安装
python run.py --version
```

预期输出：
```
agentpack version 1.0.0
```

#### 步骤 2：运行自检

```bash
python run.py --selftest
```

预期输出（部分）：
```
[PASS] 关键词路由模块
[PASS] 批量任务编排模块
[PASS] 安全预演模块
[PASS] 原子化写入模块
[PASS] 全部自检通过
```

#### 步骤 3：使用关键词路由

```bash
python run.py "清理缓存"
```

内部流程：
1. 解析输入文本 `清理缓存`
2. 匹配关键词 `缓存` → 路由到 `cache_clean` 函数
3. 执行缓存清理（默认 dry-run 模式）
4. 输出将要清理的文件清单

#### 步骤 4：批量任务编排

```bash
python run.py --batch "backup_config,clean_cache,generate_report"
```

内部流程：
1. 按逗号分隔任务列表
2. 按顺序执行：先备份配置 → 再清理缓存 → 最后生成报告
3. 任一任务失败，后续任务不执行（默认策略）

#### 步骤 5：安全预演

```bash
python run.py "清理缓存" --dry-run
```

输出示例：
```
[预演模式] 以下文件将被删除：
  /tmp/cache/a.tmp (2.3KB)
  /tmp/cache/b.tmp (1.1KB)
  /tmp/cache/sub/c.tmp (4.5KB)
共 3 个文件，合计 7.9KB
[预演完成] 未执行任何实际删除操作
```

#### 步骤 6：原子化写入

```bash
python run.py --atomic-write /path/to/config.json '{"key": "value"}'
```

内部流程：
1. 将内容写入临时文件 `/path/to/config.json.tmp`
2. 校验临时文件完整性（JSON 格式校验）
3. 使用 `os.replace()` 原子替换原文件
4. 若校验失败，删除临时文件，原文件不受影响

### 3.3 输出规范

| 输出类型 | 格式 | 示例 |
|---------|------|------|
| 版本信息 | `agentpack version <版本号>` | `agentpack version 1.0.0` |
| 自检结果 | `[PASS]/[FAIL] 模块名` | `[PASS] 关键词路由模块` |
| 预演输出 | `[预演模式] 描述` | `[预演模式] 以下文件将被删除：` |
| 错误信息 | `[错误] 错误码: 描述` | `[错误] E1001: 输入文本为空` |
| 成功提示 | `[完成] 操作描述` | `[完成] 缓存清理完成，释放 7.9KB 空间` |

---

## 四、置信度门控

### 4.1 信息不足时的处理

当输入信息不足以完成某项操作时，工具会输出 `[需核实:字段]` 占位符，**不会**编造数据。

| 场景 | 占位符示例 | 用户需补充的信息 |
|------|-----------|----------------|
| 未指定缓存目录 | `[需核实:缓存目录路径]` | 提供具体的目录路径 |
| 未指定任务列表 | `[需核实:任务列表]` | 提供逗号分隔的任务名 |
| 未指定写入目标 | `[需核实:目标文件路径]` | 提供完整的文件路径 |
| 未指定写入内容 | `[需核实:写入内容]` | 提供要写入的 JSON 字符串 |

### 4.2 置信度分级

| 置信度级别 | 判定条件 | 输出行为 |
|-----------|---------|---------|
| 高（≥90%） | 所有必要参数齐全，路径存在，权限正常 | 正常执行并输出结果 |
| 中（60-89%） | 部分参数缺失，但可通过默认值推断 | 输出警告 + 使用默认值执行 |
| 低（<60%） | 关键参数缺失或冲突 | 输出 `[需核实:字段]`，暂停执行 |

---

## 五、错误码体系

### 5.1 错误码总表

| 错误码 | 含义 | 提示话术 | 修正步骤 |
|--------|------|---------|---------|
| E1001 | 输入文本为空 | `[错误] E1001: 输入文本为空，请提供任务描述` | 重新输入包含关键词的文本 |
| E1002 | 未匹配到任何关键词 | `[错误] E1002: 无法识别任务类型，请检查关键词` | 使用 `--help` 查看支持的关键词列表 |
| E2001 | 任务列表为空 | `[错误] E2001: 批量任务列表为空` | 提供非空的逗号分隔任务列表 |
| E2002 | 任务不存在 | `[错误] E2002: 任务 'xxx' 未注册` | 检查任务名拼写，使用 `--list-tasks` 查看可用任务 |
| E3001 | 缓存目录不存在 | `[错误] E3001: 缓存目录不存在: /path/to/cache` | 创建目录或指定正确的路径 |
| E3002 | 权限不足 | `[错误] E3002: 无权限访问: /path/to/cache` | 使用 `sudo` 或调整目录权限 |
| E4001 | 写入内容格式错误 | `[错误] E4001: JSON 格式校验失败` | 检查 JSON 语法，使用在线校验工具 |
| E4002 | 原子写入失败 | `[错误] E4002: 原子替换失败: 目标文件被占用` | 关闭占用进程后重试 |
| E5001 | 自检失败 | `[错误] E5001: 自检失败，请检查环境依赖` | 查看详细日志，确认 Python 版本和依赖 |

### 5.2 错误处理流程

```
遇到错误
  ↓
输出错误码和提示话术
  ↓
用户根据修正步骤操作
  ↓
重新执行命令
  ↓
若仍失败 → 查看详细日志（--debug 参数）
```

---

## 六、FAQ 反模式

### 6.1 常见坑与反模式对照

| 常见坑 | 反模式（错误做法） | 正确做法 |
|--------|-------------------|---------|
| 直接删除缓存 | 不加 `--dry-run` 直接执行清理 | 先预演，确认文件清单后再实际执行 |
| 批量任务无依赖管理 | 任务间有依赖关系却并行执行 | 使用 `--batch` 按顺序执行，或显式声明依赖 |
| 覆盖配置文件 | 直接写入原文件，中途崩溃导致文件损坏 | 使用 `--atomic-write` 原子写入 |
| 忽略权限检查 | 不检查目录权限直接操作 | 先运行 `--selftest` 检测权限 |
| 任务名拼写错误 | 凭记忆输入任务名，报错后反复尝试 | 使用 `--list-tasks` 查看可用任务列表 |

### 6.2 反模式对照表（详细）

| 反模式 | 问题描述 | 推荐替代方案 |
|--------|---------|-------------|
| `python run.py "清理"` | 关键词"清理"过于宽泛，可能匹配多个任务 | 使用更具体的关键词，如"清理缓存"或"清理日志" |
| `python run.py --batch "task1,task2"` 且 task1 失败 | 默认策略会中断后续任务，用户可能期望继续 | 添加 `--continue-on-error` 参数（需自行扩展） |
| 手动编辑配置文件 | 直接使用文本编辑器修改，无备份无校验 | 使用 `--atomic-write` 确保安全 |
| 在生产环境直接测试 | 未在测试环境验证就执行清理 | 先使用 `--dry-run` 预演，再在测试环境验证 |

---

## 七、渐进式披露

### 7.1 速查卡（30 秒上手）

```bash
# 查看版本
python run.py --version

# 自检
python run.py --selftest

# 清理缓存（预演模式）
python run.py "清理缓存" --dry-run

# 清理缓存（实际执行）
python run.py "清理缓存"

# 批量任务
python run.py --batch "backup_config,clean_cache"

# 原子写入
python run.py --atomic-write config.json '{"key": "value"}'
```

### 7.2 新手阅读路径（5 分钟）

1. **先读**：第一章「能力边界」→ 了解工具能做什么、不能做什么
2. **再读**：第三章「标准流程」步骤 1-2 → 完成安装和自检
3. **然后**：第三章「标准流程」步骤 3 → 尝试第一个清理任务
4. **最后**：第五章「错误码体系」→ 遇到问题时查阅

### 7.3 进阶阅读路径（15 分钟）

1. **深入**：第三章「标准流程」步骤 4-6 → 掌握批量编排、预演、原子写入
2. **扩展**：阅读 `run.py` 源码中的 `TASK_REGISTRY` 字典 → 了解如何注册自定义任务
3. **定制**：修改 `run.py` 中的 `route_keywords` 字典 → 添加自定义关键词路由
4. **调试**：使用 `--debug` 参数运行 → 查看详细执行日志

---

## 八、参数参考表

### 8.1 CLI 参数总览

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `--version` | 标志 | 否 | - | 输出版本号 |
| `--selftest` | 标志 | 否 | - | 运行自检 |
| `--dry-run` | 标志 | 否 | `False` | 预演模式，不实际执行写操作 |
| `--batch` | 字符串 | 否 | - | 逗号分隔的任务列表 |
| `--atomic-write` | 字符串 | 否 | - | 目标文件路径（需配合内容参数） |
| `--debug` | 标志 | 否 | `False` | 输出详细调试日志 |
| `--list-tasks` | 标志 | 否 | - | 列出所有已注册任务 |
| `--help` | 标志 | 否 | - | 显示帮助信息 |

### 8.2 关键词路由表（默认）

| 关键词 | 路由目标 | 说明 |
|--------|---------|------|
| 缓存 / cache | `cache_clean` | 清理缓存目录 |
| 日志 / log | `log_clean` | 清理日志文件 |
| 备份 / backup | `backup_config` | 备份配置文件 |
| 报告 / report | `generate_report` | 生成执行报告 |
| 临时 / temp | `temp_clean` | 清理临时文件 |

---

## 九、用户协议

<!-- user-agreement-injected -->

**使用 agentpack 即表示您同意以下条款：**

1. **责任承担**：使用者自行承担因使用本工具产生的全部责任。包括但不限于因误删文件、配置错误、数据丢失等造成的直接或间接损失。
2. **禁止反向工程**：不得对本工具进行反向工程、反编译、破解或试图提取源代码（除非适用法律允许）。
3. **无担保声明**：本工具按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性。
4. **使用限制**：不得将本工具用于任何非法目的，或违反您所在司法管辖区法律法规的活动。
5. **修改与分发**：在保留版权声明的前提下，允许修改和分发本工具，但须注明原始出处。

---

## 十、许可证（License）

<!-- professional-license-embedded -->

### MIT License

版权所有 (c) 2025 Lin Chen

特此免费授予任何获得本软件及相关文档文件（以下简称"软件"）副本的人士，不受限制地处理本软件，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或销售软件副本的权利，并允许向其提供软件的人士这样做，但须满足以下条件：

上述版权声明和本许可声明应包含在本软件的所有副本或主要部分中。

本软件按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性。在任何情况下，作者或版权持有人均不对任何索赔、损害或其他责任负责，无论是合同诉讼、侵权行为还是其他原因，因本软件或本软件的使用或其他交易而产生或与之相关。

---

*本 Skill 由 AI 辅助生成，仅供参考。使用前请阅读相关文档，并自行承担使用风险。*
