---
<!-- © 2026 SkillForge Lab. All rights reserved. -->
slug: agentpack
name: agentpack
displayName: 智能调度 缓存清理 批量路由
description: 基于关键词路由与缓存清理的智能体任务调度工具，提升批量操作效率。
version: 2.0.4
rules_version: cpr-20260813-n401
license: MIT
source_project: original
source_url: https://github.com/bluebigman/skill-factory-originals/tree/main/agentpack
copyright_holder: 原创作者（自持版权）
ai_generated: true
ai_tools: ["DeepSeek"]
disclaimer: 本Skill由AI辅助生成，提供使用指导和最佳实践。使用前请阅读相关文档。
author: LinguaForge
agent_created: true
trigger_words: ["清理", "分析", "批量", "调度", "路由", "缓存清理", "任务编排", "临时文件管理"]
---

> ⚠️ **本内容仅供一般信息参考，不构成法律、财务、税务、投资或医疗建议。**
> 涉及合同签署、报税、投资、诊疗等专业决策时，请务必咨询持证专业人士，并由使用者自行承担决策后果。
<!-- professional-disclaimer-injected -->


> 本内容由 AI 生成，仅供学习参考
<!-- ai-generated-notice -->

# agentpack — 智能体任务调度与缓存清理工具

## 一、能力边界（一页纸速查卡）

### 1.1 能做什么

| 能力项 | 说明 | 示例 |
|--------|------|------|
| 关键词路由 | 根据输入文本中的关键词，自动匹配并执行对应任务 | 输入"清理缓存" → 触发清理任务 |
| 批量任务调度 | 使用分号 `;` 串联多个子任务，按顺序依次执行 | `agentpack 清理;分析` → 先清理再分析 |
| 缓存清理 | 删除指定临时文件或清空目录 | `agentpack 清理` → 删除 `./tmp/` 下文件 |
| 预演预览 | 通过 `--dry-run` 模式查看将要执行的操作，不实际变更 | `agentpack --dry-run 清理` |
| 环境自检 | 验证工具是否正常工作 | `agentpack --selftest` |
| 多项目隔离 | 通过 `--project-dir` 指定项目目录，实现隔离管理 | `agentpack --project-dir /path/to/project 清理` |

### 1.2 不能做什么

| 限制项 | 说明 |
|--------|------|
| 不支持模糊语义理解 | 仅基于精确关键词匹配，不进行语义分析 |
| 不处理非临时文件 | 仅操作 `./tmp/` 目录下的临时文件，不触碰业务数据 |
| 不支持并行执行 | 子任务按顺序串行执行，不提供并发能力 |
| 不提供图形界面 | 纯命令行工具，无 GUI |

### 1.3 适用对象

- 需要定期清理临时文件的开发人员
- 需要批量执行多个任务的运维工程师
- 希望在 CI/CD 流程中集成自动化清理的团队

---

## 二、触发方式

### 2.1 触发词

| 触发词 | 触发场景 |
|--------|----------|
| 清理 | 需要删除临时文件或清空缓存目录时 |
| 分析 | 需要对文件或目录进行统计分析时 |
| 批量 | 需要同时执行多个任务时 |
| 调度 | 需要按顺序编排任务执行时 |
| 路由 | 需要根据关键词自动分发任务时 |
| 缓存清理 | 同"清理"，更明确的表达 |
| 任务编排 | 同"调度"，更明确的表达 |
| 临时文件管理 | 同"清理"，更明确的表达 |

### 2.2 场景映射表

| 用户说（大白话） | 实际执行 |
|------------------|----------|
| "帮我清一下临时文件" | `agentpack 清理` |
| "把缓存都删了" | `agentpack 清理` |
| "先清理再分析一下" | `agentpack 清理;分析` |
| "看看清理会删哪些文件" | `agentpack --dry-run 清理` |
| "检查一下工具能不能用" | `agentpack --selftest` |

---

## 三、标准流程

### 3.1 前置条件

| 条件 | 检查方式 | 通过标准 |
|------|----------|----------|
| 工具已安装 | `which agentpack` | 返回可执行文件路径 |
| 版本正常 | `agentpack --version` | 输出版本号，无报错 |
| 环境自检通过 | `agentpack --selftest` | 输出 `OK` 或类似成功标识 |

### 3.2 执行步骤

1. **验证环境**：运行 `agentpack --version && agentpack --selftest`，确认工具可用。
2. **预演预览**：运行 `agentpack --dry-run <操作>`，查看将要执行的操作列表。
3. **确认操作**：检查预演输出，确认无误。
4. **正式执行**：运行 `agentpack <操作>`，系统会提示确认。
5. **输入确认**：
   - 输入 `y`：正式执行操作
   - 输入 `n`：取消操作，不产生任何变更
   - 输入其他字符：重新提示，直到输入 `y` 或 `n`

### 3.3 输出规范

| 操作类型 | 输出内容 |
|----------|----------|
| `--version` | 版本号，格式如 `agentpack v1.0.0` |
| `--selftest` | 各检查项结果，每项一行，最终输出 `OK` |
| `--dry-run` | 将要执行的操作列表，每行一条，格式如 `[待删除] ./tmp/cache_20240101.bin (2.3 MB)` |
| 正式执行 | 实际执行结果，每行一条，格式如 `[已删除] ./tmp/cache_20240101.bin (2.3 MB)` |

---

## 四、关键词路由规则

### 4.1 匹配优先级

| 优先级 | 规则 | 示例 |
|--------|------|------|
| 1 | 精确匹配优先于部分匹配 | 输入"清理缓存" → 精确匹配"清理"任务 |
| 2 | 多关键词组合时，按优先级从高到低执行 | 输入"清理 分析" → 先执行"清理"，再执行"分析" |
| 3 | 分号分隔的子任务按顺序执行 | 输入"清理;分析" → 先执行"清理"，再执行"分析" |

### 4.2 关键词与任务映射

| 关键词 | 对应任务 | 优先级 |
|--------|----------|--------|
| 清理 | 删除临时文件 | 高 |
| 分析 | 统计分析 | 中 |
| 批量 | 批量执行 | 中 |
| 调度 | 任务编排 | 中 |
| 路由 | 任务分发 | 低 |

---

## 五、置信度门控

当输入信息不足以确定执行意图时，工具会输出 `[需核实:字段]` 占位符，不会擅自猜测执行。

| 场景 | 输出示例 |
|------|----------|
| 未指定操作类型 | `[需核实:操作类型] 请指定要执行的操作，如"清理"或"分析"` |
| 未指定项目目录 | `[需核实:项目目录] 请使用 --project-dir 指定项目目录` |
| 关键词冲突 | `[需核实:任务优先级] 检测到多个关键词，请明确执行顺序` |

---

## 六、错误码体系

| 错误码 | 含义 | 提示话术 | 修正步骤 |
|--------|------|----------|----------|
| E001 | 工具未安装 | `agentpack: command not found` | 安装 agentpack 后重试 |
| E002 | 环境自检失败 | `agentpack: selftest failed` | 检查依赖项，重新安装 |
| E003 | 无效操作 | `agentpack: unknown operation` | 使用 `agentpack --help` 查看支持的操作 |
| E004 | 目录不存在 | `agentpack: directory not found` | 检查 `--project-dir` 路径是否正确 |
| E005 | 权限不足 | `agentpack: permission denied` | 使用 `sudo` 或检查文件权限 |
| E006 | 确认超时 | `agentpack: confirmation timeout` | 重新执行命令，及时输入 `y` 或 `n` |

---

## 七、FAQ 反模式

### 7.1 常见坑

| 坑 | 反模式 | 正确做法 |
|----|--------|----------|
| 跳过预演 | 直接执行正式操作，导致误删文件 | 先执行 `--dry-run` 预览，确认无误后再正式执行 |
| 忽略自检 | 跳过 `--selftest`，遇到环境问题无法定位 | 每次使用前先运行 `--selftest` |
| 关键词歧义 | 输入"清理分析"未加分隔符，导致匹配混乱 | 使用分号 `;` 分隔多个任务，如"清理;分析" |
| 跨项目操作 | 未指定 `--project-dir`，误操作其他项目 | 始终使用 `--project-dir` 明确目标目录 |
| 批量无确认 | 批量任务未逐项确认，导致意外变更 | 批量任务执行前，使用 `--dry-run` 逐项检查 |

### 7.2 反模式对照表

| 反模式 | 问题 | 替代方案 |
|--------|------|----------|
| `agentpack 清理` 直接执行 | 无预演，风险高 | `agentpack --dry-run 清理` 先预览 |
| `agentpack 清理 分析` 无分隔 | 关键词匹配混乱 | `agentpack 清理;分析` 分号分隔 |
| 不检查版本直接使用 | 版本不兼容导致异常 | 先 `agentpack --version` 确认版本 |

---

## 八、渐进式披露

### 8.1 速查卡（新手必读）

```bash
# 1. 验证环境
agentpack --version
agentpack --selftest

# 2. 预演预览
agentpack --dry-run 清理

# 3. 正式执行
agentpack 清理
# 输入 y 确认，输入 n 取消

# 4. 批量操作
agentpack 清理;分析
```

### 8.2 分层次阅读路径

| 读者层级 | 建议阅读章节 | 学习目标 |
|----------|--------------|----------|
| 新手 | 一、二、三、八.1 | 掌握基本使用流程，能完成简单清理任务 |
| 进阶 | 四、五、六、七 | 掌握关键词路由规则，能设计复杂批量任务 |
| 高级 | 八.3 | 能结合 CI/CD 实现自动化，自定义路由规则 |

### 8.3 高级用法

#### 8.3.1 设计复杂的批量调度任务

```bash
# 按优先级执行多个任务
agentpack "清理;分析;调度"

# 结合项目目录隔离
agentpack --project-dir /path/to/project-a "清理;分析"
agentpack --project-dir /path/to/project-b "清理;分析"
```

#### 8.3.2 结合 CI/CD 流程

```yaml
# .github/workflows/cleanup.yml 示例
jobs:
  cleanup:
    steps:
      - name: Verify agentpack
        run: agentpack --version && agentpack --selftest
      - name: Dry run
        run: agentpack --dry-run 清理
      - name: Execute cleanup
        run: agentpack 清理
```

#### 8.3.3 自定义路由规则

通过配置文件扩展关键词映射：

```yaml
# agentpack.config.yaml
routes:
  - keywords: ["清空", "删除缓存"]
    task: cleanup
  - keywords: ["统计", "报告"]
    task: analyze
```

#### 8.3.4 编写脚本封装常用操作

```bash
#!/bin/bash
# cleanup.sh — 一键清理脚本
agentpack --selftest || exit 1
agentpack --dry-run 清理
read -p "确认执行清理？(y/n): " confirm
if [ "$confirm" = "y" ]; then
  agentpack 清理
else
  echo "已取消"
fi
```

---

## 九、用户协议

<!-- user-agreement-injected -->

**使用 agentpack 即表示您同意以下条款：**

1. **责任承担**：使用者自行承担使用本工具的全部责任。因使用本工具导致的任何直接或间接损失，包括但不限于数据丢失、系统故障、业务中断等，工具作者及贡献者不承担任何责任。
2. **禁止反向工程**：禁止对本工具进行反向工程、反编译、反汇编或试图提取源代码（除非适用法律允许）。
3. **合规使用**：使用者应确保使用本工具的行为符合当地法律法规及所在组织的规章制度。
4. **无担保**：本工具按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性担保。

---

## 十、许可证（License）

<!-- professional-license-embedded -->

**MIT License**

版权所有 (c) 2024 LinguaForge

特此免费授予任何获得本软件及相关文档文件（"软件"）副本的人，不受限制地处理本软件，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或出售软件副本的权利，并允许向其提供软件的人这样做，但须满足以下条件：

上述版权声明和本许可声明应包含在软件的所有副本或主要部分中。

本软件按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性担保。在任何情况下，作者或版权持有人均不对因使用本软件而产生的任何索赔、损害或其他责任负责，无论是在合同、侵权或其他方面。

---

*本 Skill 由 AI 辅助生成，仅供参考。使用前请阅读相关文档。*
