---
slug: ai-desktop-client-builder
name: ai-desktop-client-builder
displayName: 桌面客户端 会话编辑器 Git集成 构建工具
description: 为AI编程CLI构建一体化桌面客户端，集成会话、编辑器与Git操作。
version: 1.0.0
license: MIT
source_project: original
source_url: 
copyright_holder: 原创作者（自持版权）
ai_generated: true
ai_tools: ["DeepSeek"]
disclaimer: 本Skill由AI辅助生成，提供使用指导和最佳实践。使用前请阅读相关文档。
author: 独立技能工坊
agent_created: true
trigger_words: ["ai-desktop-client-builder", "桌面客户端构建", "AI编程CLI客户端", "会话编辑器Git集成", "一体化桌面工具"]
---

> 本内容由 AI 生成，仅供学习参考
<!-- ai-generated-notice -->

# AI 桌面客户端构建器（AI Desktop Client Builder）

## 一、能力边界：一页纸速查卡

### 1.1 能做什么

| 能力项 | 说明 | 关键参数/约束 |
|--------|------|---------------|
| 会话管理 | 创建、切换、持久化 AI 编程 CLI 会话 | 会话数上限 100，自动保存间隔 30s |
| 编辑器集成 | 内嵌代码编辑器，支持语法高亮与文件树 | 单文件最大 10MB，支持 UTF-8 编码 |
| Git 操作 | 查看状态、提交、推送、拉取、分支管理 | 仅支持 HTTPS 远程仓库，SSH 需额外配置 |
| 命令透传 | 将用户输入透传给底层 CLI 并捕获输出 | 命令超时默认 60s，可配置 10-300s |
| 日志审计 | 记录操作日志与错误日志 | 日志保留 7 天，单文件 5MB 轮转 |

### 1.2 不能做什么

| 禁止项 | 原因 | 替代方案 |
|--------|------|----------|
| 存储密码/API 密钥 | 敏感信息保护要求 | 使用系统钥匙串或环境变量 |
| 访问白名单以外的路径 | 路径安全要求 | 在设置中添加白名单路径 |
| 无超时的网络请求 | 网络请求要求 | 所有请求强制设置超时与重试 |
| 一次性加载超大文件（>100MB） | 大文件处理要求 | 使用流式读取，分段加载 |
| 绕过异常捕获直接调用外部服务 | 异常捕获要求 | 所有外部调用必须 try-except 包裹 |

### 1.3 适用对象

- **初级用户**：需要图形界面操作 AI 编程 CLI，不熟悉命令行
- **中级用户**：希望在同一窗口内完成编辑、会话与 Git 操作
- **高级用户**：需要自定义命令透传与批量任务处理

---

## 二、触发方式：场景映射表

| 触发词/场景 | 用户意图 | 响应动作 |
|-------------|----------|----------|
| "打开桌面客户端" | 启动应用 | 初始化主窗口，加载最近会话 |
| "新建会话" | 开始新的 AI 编程对话 | 创建会话，清空编辑器，重置 Git 面板 |
| "查看 Git 状态" | 检查当前仓库变更 | 刷新 Git 面板，显示 status 结果 |
| "提交代码" | 执行 Git commit | 弹出提交信息输入框，执行提交 |
| "推送远程" | 执行 Git push | 确认远程地址后执行推送 |
| "编辑文件" | 打开文件进行修改 | 在编辑器中加载文件内容 |
| "运行命令" | 透传命令给底层 CLI | 在命令输入框执行并显示输出 |
| "查看日志" | 检查操作记录 | 打开日志面板，按时间倒序显示 |

---

## 三、标准流程

### 3.1 前置条件

| 条件 | 检查方式 | 失败处理 |
|------|----------|----------|
| 底层 CLI 已安装 | 执行 `--version` 命令 | 提示安装指引，退出启动流程 |
| 工作目录存在 | 检查路径有效性 | 提示创建目录或选择其他路径 |
| Git 仓库已初始化 | 执行 `git rev-parse --git-dir` | 提示 `git init` 或克隆仓库 |
| 网络可用（如需远程操作） | 发送 ping 或检查连接 | 提示离线模式，禁用远程功能 |

### 3.2 执行步骤（标准启动流程）

1. **初始化配置**：读取配置文件 `~/.ai-desktop-client/config.json`，加载白名单路径、超时参数、日志级别。
2. **校验环境**：执行 `cli --version` 确认底层 CLI 可用，检查 Git 版本（需 ≥ 2.20）。
3. **加载会话列表**：从 `~/.ai-desktop-client/sessions/` 目录读取会话元数据，按最后修改时间排序。
4. **启动主窗口**：创建三栏布局（左侧文件树、中间编辑器、右侧会话/Git 面板）。
5. **连接 CLI 进程**：启动子进程，建立 stdin/stdout 管道，设置超时与重试参数。
6. **恢复上次会话**：若存在未关闭会话，自动加载最后活跃会话的上下文。

### 3.3 输出规范

| 输出类型 | 格式要求 | 示例 |
|----------|----------|------|
| 会话输出 | 等宽字体，保留原始换行与缩进 | `$ git status` 的完整输出 |
| 错误提示 | 红色高亮，包含错误码与建议 | `[E1001] 命令超时，请检查网络或增加超时时间` |
| 操作确认 | 弹窗或内联确认按钮 | `确认推送到 origin/main？` |
| 进度指示 | 进度条或旋转指示器 | 推送中... 45% |

---

## 四、置信度门控

### 4.1 信息不足时的处理

当遇到以下情况时，输出 `[需核实:字段]` 占位符，不进行猜测：

| 场景 | 占位符示例 | 后续动作 |
|------|------------|----------|
| 远程仓库地址未知 | `[需核实:remote_url]` | 提示用户手动输入或从配置读取 |
| 分支名称不确定 | `[需核实:branch_name]` | 提示用户从下拉列表选择 |
| 提交者身份未知 | `[需核实:user_name]` | 读取 Git 全局配置，若缺失则提示 |
| 文件编码不确定 | `[需核实:file_encoding]` | 尝试 UTF-8 解析，失败则提示选择 |

### 4.2 禁止行为

- 不编造命令输出内容
- 不假设用户未提供的参数值
- 不自动执行可能产生破坏性后果的命令（如 `git push --force`）

---

## 五、错误码体系

| 错误码 | 含义 | 提示话术 | 修正步骤 |
|--------|------|----------|----------|
| E1001 | 命令超时 | 命令执行超过设定时间，已终止 | 1. 检查网络连接；2. 增加超时时间；3. 重试 |
| E1002 | CLI 未安装 | 未检测到底层 CLI，请先安装 | 1. 前往官网下载；2. 安装后重启应用 |
| E1003 | Git 仓库无效 | 当前目录不是有效的 Git 仓库 | 1. 执行 `git init`；2. 或选择其他目录 |
| E2001 | 路径越权 | 请求的路径不在白名单内 | 1. 在设置中添加该路径；2. 重新操作 |
| E2002 | 文件过大 | 文件超过 10MB 限制 | 1. 使用外部编辑器；2. 或分段处理 |
| E3001 | 网络请求失败 | 无法连接到远程服务器 | 1. 检查网络；2. 确认 URL 正确；3. 重试（最多 3 次） |
| E4001 | 敏感信息泄露风险 | 检测到输入包含疑似密钥内容 | 1. 停止输入；2. 使用环境变量替代 |
| E5001 | 内部异常 | 发生未预期错误 | 1. 查看日志文件；2. 重启应用；3. 反馈问题 |

---

## 六、FAQ 反模式

### 6.1 常见坑与反模式对照

| 常见坑 | 反模式（错误做法） | 正模式（推荐做法） |
|--------|-------------------|-------------------|
| 密码明文存储 | 在配置文件中写入 API 密钥 | 使用系统钥匙串或环境变量 |
| 路径穿越攻击 | 直接拼接用户输入路径 | 先校验路径是否在白名单内 |
| 大文件卡死 | 用 `read()` 一次性读取 500MB 文件 | 使用流式读取，分块处理 |
| 网络请求无响应 | 不设置超时，无限等待 | 设置 10s 超时，失败重试 3 次 |
| 异常被吞掉 | `except: pass` 静默处理 | 记录完整错误日志并提示用户 |
| 并发请求过多 | 无限制创建线程 | 使用线程池，最大并发 5 |

### 6.2 反模式示例

```python
# 反模式：直接拼接路径
file_path = os.path.join(base_dir, user_input)  # 可能路径穿越

# 正模式：白名单校验
if not is_path_allowed(file_path, whitelist):
    raise PermissionError("路径不在白名单内")
```

---

## 七、渐进式披露

### 7.1 速查卡（30 秒上手）

```
启动 → 选择工作目录 → 新建会话 → 输入命令 → 查看输出 → 提交代码
```

### 7.2 新手路径（首次使用）

1. 阅读「能力边界」了解能做什么
2. 按「标准流程」启动应用
3. 尝试新建会话并输入 `--help` 查看 CLI 帮助
4. 遇到问题查「错误码体系」

### 7.3 进阶路径（熟练用户）

1. 配置自定义超时与重试参数
2. 使用批量命令透传（分号分隔多条命令）
3. 设置 Git 钩子自动化提交前检查
4. 编写插件扩展编辑器功能

---

## 八、安全与性能规范

### 8.1 敏感信息保护

| 信息类型 | 存储方式 | 访问控制 |
|----------|----------|----------|
| API 密钥 | 系统钥匙串 | 仅当前用户可读 |
| 密码 | 不存储 | 每次手动输入 |
| 会话内容 | 本地加密存储 | 应用启动时解锁 |

### 8.2 路径安全

- 白名单默认包含：`~/.ai-desktop-client/`、`~/projects/`、`/tmp/`
- 路径校验规则：解析符号链接后检查是否在白名单内
- 禁止使用 `..` 跳转出白名单目录

### 8.3 网络请求规范

| 参数 | 默认值 | 可配置范围 |
|------|--------|------------|
| 连接超时 | 5s | 1-30s |
| 读取超时 | 10s | 5-60s |
| 重试次数 | 3 | 0-5 |
| 重试间隔 | 1s | 0.5-5s |

### 8.4 大文件处理

- 编辑器单文件限制：10MB
- 超过 10MB 提示使用外部编辑器
- 日志文件超过 5MB 自动轮转

### 8.5 缓存策略

| 缓存对象 | 缓存时间 | 失效策略 |
|----------|----------|----------|
| 文件树 | 30s | 文件系统变更时失效 |
| Git 状态 | 10s | 手动刷新或操作后失效 |
| 会话列表 | 60s | 新建/删除会话时失效 |

### 8.6 并发控制

- 线程池大小：5（可配置 1-10）
- 命令执行队列：最多 10 个等待任务
- 超出队列时提示"系统繁忙，请稍后重试"

---

## 九、运维建议

### 9.1 生产环境试用

- 建议先在个人项目或小团队中试用 1-2 周
- 观察日志中是否有异常错误
- 确认所有核心功能稳定后再推广

### 9.2 数据备份

| 数据 | 备份频率 | 保留策略 |
|------|----------|----------|
| 会话数据 | 每日 | 保留 30 天 |
| Git 仓库 | 每周 | 保留 4 周 |
| 配置文件 | 每次修改 | 保留 10 个版本 |

### 9.3 版本升级

- 升级前查看更新日志，确认兼容性
- 备份配置与数据
- 升级后运行 `--selftest` 验证功能完整性

---

## 十、用户协议

<!-- user-agreement-injected -->

**使用本 Skill 即表示您同意以下条款：**

1. **责任承担**：使用者自行承担使用本 Skill 的全部责任。因使用本 Skill 导致的任何直接或间接损失，包括但不限于数据丢失、业务中断、利润损失等，作者不承担任何责任。
2. **禁止反向工程**：不得对本 Skill 进行反向工程、反编译、反汇编或试图提取源代码。
3. **合法使用**：使用者应确保使用本 Skill 的行为符合当地法律法规，不得用于任何非法目的。
4. **无担保声明**：本 Skill 按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性。
5. **协议更新**：作者保留随时修改本协议的权利，修改后的协议将在本页面公布。

---

## 十一、许可证（License）

<!-- professional-license-embedded -->

**MIT License**

版权所有 (c) 2024 独立技能工坊

特此免费授予任何获得本软件及相关文档文件（以下简称"软件"）副本的人士处理本软件的权利，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或销售软件副本的权利，并允许向软件提供对象授予上述权利，但须满足以下条件：

上述版权声明和本许可声明应包含在本软件的所有副本或实质性部分中。

本软件按"现状"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性。在任何情况下，作者或版权持有人均不对任何索赔、损害或其他责任负责，无论是在合同诉讼、侵权或其他方面，由本软件或本软件的使用或其他交易引起、产生或与之相关。

---

*本 Skill 由 AI 辅助生成，仅供参考。使用前请阅读相关文档并自行验证功能。*
