---
<!-- © 2026 SkillForge Lab. All rights reserved. -->
slug: ai-short-drama-agent-company
name: 短剧工厂编排
displayName: 短剧工厂 团队编排 流程管理
description: 短剧制作团队角色与流程编排，输出标准节点与协作协议。
version: 1.0.1
rules_version: cpr-20260810-n301
license: MIT
source_project: original
source_url: https://github.com/bluebigman/skill-factory-originals/tree/main/ai-short-drama-agent-company
copyright_holder: 原创作者（自持版权）
ai_generated: true
ai_tools: ["DeepSeek"]
disclaimer: 本Skill由AI辅助生成，提供使用指导和最佳实践。使用前请阅读相关文档。
author: FlowForge Studio
agent_created: true
trigger_words: ["短剧团队编排", "短剧流程管理", "短剧制作SOP", "短剧角色定义", "短剧协作协议", "短剧预算估算"]

> 本内容由 AI 生成，仅供学习参考
<!-- ai-generated-notice -->

# 短剧工厂编排 Skill 文档

## 一、能力边界：一页纸速查卡

### 1.1 本 Skill 能做什么

| 编号 | 能力项 | 说明 | 输出物 |
|------|--------|------|--------|
| C1 | 团队角色编排 | 输出 5 个专业角色的完整定义，包含职责范围、输入要求、输出格式、协作对象 | 角色定义表 |
| C2 | 流程节点管理 | 输出 12 个标准节点的定义、执行顺序、依赖关系 | 节点依赖图 |
| C3 | 交付物模板 | 为每个节点匹配对应的交付物模板名称 | 模板映射表 |
| C4 | 跨角色协作协议 | 定义交接格式、评审机制、协作规范 | 协作协议文档 |
| C5 | 预算与资源估算 | 输出分环节成本估算参数表与总预算范围 | 预算估算表 |
| C6 | 流程编排引擎 | 验证节点依赖关系，检测循环依赖与缺失依赖 | 验证报告 |
| C7 | 内置自检 | 验证所有核心功能与数据一致性 | 自检报告 |

### 1.2 本 Skill 不能做什么

| 编号 | 限制项 | 说明 |
|------|--------|------|
| L1 | 创意内容生成 | 不生成剧本、分镜、台词等创意内容 |
| L2 | 真实项目管理 | 不跟踪项目进度、任务分配、截止日期 |
| L3 | 财务核算 | 不进行真实成本核算、发票管理、税务计算 |
| L4 | 网络服务 | 不访问任何外部 API 或服务 |
| L5 | 文件读写 | 不读取或修改用户文件（所有数据均为内置） |

### 1.3 适用对象

- 短剧制作团队的制片人、导演、编剧组长
- 需要快速搭建制作流程 SOP 的中小团队
- 影视相关专业的学生与研究者
- 对短剧工业化生产流程感兴趣的产品经理

---

> ⚠️ **本内容仅供一般信息参考，不构成法律、财务、税务、投资或医疗建议。**
> 涉及合同签署、报税、投资、诊疗等专业决策时，请务必咨询持证专业人士，并由使用者自行承担决策后果。
<!-- professional-disclaimer-injected -->


## 二、触发方式

### 2.1 触发词

- 主触发词：短剧团队编排、短剧流程管理、短剧制作SOP
- 辅助触发词：短剧角色定义、短剧协作协议、短剧预算估算
- 扩展触发词：微短剧流程设计、短剧生产管线、短剧岗位职责

### 2.2 场景映射表

| 用户说（大白话） | 触发能力 | 预期输出 |
|-----------------|----------|----------|
| "我想搭一个短剧制作团队，需要哪些岗位？" | C1 | 5 个角色的完整定义 |
| "短剧制作的标准流程是什么？" | C2 | 12 个节点的流程定义 |
| "每个环节要产出什么文档？" | C3 | 交付物模板映射表 |
| "团队之间怎么交接？" | C4 | 协作协议与交接格式 |
| "拍一部短剧大概要花多少钱？" | C5 | 预算估算参数表 |
| "帮我检查一下流程有没有问题？" | C6 | 依赖验证报告 |
| "确认一下功能是否正常" | C7 | 自检报告 |

---

## 三、标准流程

### 3.1 前置条件

| 条件编号 | 条件内容 | 检查方式 |
|----------|----------|----------|
| P1 | 已安装 Python 3.8+ 环境 | 运行 `python --version` |
| P2 | 已获取 `run.py` 脚本 | 确认文件存在于当前目录 |
| P3 | 环境未发生变更 | 运行自检确认 |

### 3.2 执行步骤

#### 步骤 1：环境自检

在任何操作之前，先运行自检命令确认功能正常：

```bash
python run.py --selftest
```

**预期输出**：所有测试用例通过，显示 `ALL TESTS PASSED`。

**失败处理**：若自检失败，检查 Python 版本与依赖包，修复后重新运行。

#### 步骤 2：查看版本信息

```bash
python run.py --version
```

**预期输出**：`ai-short-drama-agent-company v1.0.0`

#### 步骤 3：生成团队角色编排

```bash
python run.py --roles
```

**输出内容**：
- 5 个角色的职责描述
- 每个角色的输入/输出格式
- 角色间的协作关系矩阵

#### 步骤 4：生成流程节点定义

```bash
python run.py --nodes
```

**输出内容**：
- 12 个标准节点的定义
- 节点间的依赖关系
- 执行顺序说明

#### 步骤 5：生成交付物模板映射

```bash
python run.py --templates
```

**输出内容**：每个节点对应的交付物模板名称列表

#### 步骤 6：生成协作协议

```bash
python run.py --protocol
```

**输出内容**：
- 交接格式规范
- 评审机制说明
- 协作协议条款

#### 步骤 7：预算与资源估算

```bash
python run.py --budget
```

**输出内容**：分环节成本估算参数表与总预算范围

> **注意**：`--budget` 输出的是估算值，实际成本请根据市场行情调整参数。

#### 步骤 8：流程验证

```bash
python run.py --validate
```

**输出内容**：节点依赖关系验证报告，包括循环依赖与缺失依赖检测。

> **注意**：修改节点依赖关系后，务必运行 `--validate` 确认无循环依赖。

#### 步骤 9：JSON 格式输出

```bash
python run.py --roles --format json
```

**适用场景**：需要程序化处理结果时，使用 `--format json` 便于解析。

### 3.3 输出规范

| 输出类型 | 格式 | 适用场景 |
|----------|------|----------|
| 人类可读 | Markdown 表格 | 文档编写、人工审阅 |
| 机器可读 | JSON | 程序化处理、二次开发 |
| 验证报告 | 文本 + 状态码 | CI/CD 集成 |

---

## 四、置信度门控

### 4.1 信息不足处理

当输入信息不足以生成完整输出时，使用以下占位符：

| 占位符 | 含义 | 使用场景 |
|--------|------|----------|
| `[需核实:角色名称]` | 角色名称待确认 | 用户未指定团队角色时 |
| `[需核实:预算范围]` | 预算数据待确认 | 用户未提供市场行情时 |
| `[需核实:节点数量]` | 节点数量待确认 | 用户自定义流程时 |

### 4.2 禁止行为

- 不编造不存在的角色或节点
- 不虚构预算数据
- 不猜测用户未提供的参数

---

## 五、错误码体系

| 错误码 | 错误描述 | 提示话术 | 修正步骤 |
|--------|----------|----------|----------|
| E001 | 自检失败 | "环境自检未通过，请检查 Python 版本与依赖" | 1. 检查 Python 版本 ≥ 3.8；2. 重新安装依赖；3. 重跑自检 |
| E002 | 循环依赖 | "检测到节点间存在循环依赖，请检查依赖关系" | 1. 查看验证报告；2. 定位循环路径；3. 调整依赖关系 |
| E003 | 缺失依赖 | "节点 X 缺少前置依赖节点 Y" | 1. 确认节点 Y 是否存在；2. 添加依赖关系；3. 重新验证 |
| E004 | 无效参数 | "参数 --xxx 不存在，请查看帮助文档" | 1. 运行 `python run.py --help`；2. 确认参数名称；3. 重新执行 |
| E005 | 输出格式错误 | "JSON 序列化失败，请检查数据格式" | 1. 检查数据是否含非法字符；2. 确认编码为 UTF-8；3. 重试 |

---

## 六、FAQ 反模式

### 6.1 常见坑与反模式对照

| 坑编号 | 常见错误 | 反模式示例 | 正确做法 |
|--------|----------|------------|----------|
| F1 | 跳过自检 | 直接运行 `--roles` 而不先跑 `--selftest` | 任何环境变更后先运行自检 |
| F2 | 忽视预算估算偏差 | 将 `--budget` 输出当作精确成本 | 将估算值作为参考，根据市场行情调整 |
| F3 | 修改依赖后不验证 | 调整节点依赖后直接使用 | 修改后必须运行 `--validate` |
| F4 | 手工解析文本输出 | 用正则表达式解析 Markdown 表格 | 使用 `--format json` 获取结构化数据 |
| F5 | 混淆能力边界 | 期望 Skill 生成剧本内容 | 明确本 Skill 仅做流程编排，创意内容需另行处理 |

### 6.2 反模式自查清单

- [ ] 是否在环境变更后运行了自检？
- [ ] 是否将预算估算值当作精确成本？
- [ ] 是否在修改依赖后运行了验证？
- [ ] 是否在程序化处理时使用了 JSON 输出？
- [ ] 是否期望 Skill 生成创意内容？

---

## 七、渐进式披露

### 7.1 速查卡（30 秒上手）

```
1. 运行自检：python run.py --selftest
2. 查看角色：python run.py --roles
3. 查看流程：python run.py --nodes
4. 查看预算：python run.py --budget
5. 验证流程：python run.py --validate
```

### 7.2 新手路径（首次使用）

1. 阅读本 SKILL.md 的「能力边界」章节，明确工具范围
2. 运行 `--selftest` 确认环境正常
3. 依次运行 `--roles`、`--nodes`、`--templates` 了解基础输出
4. 阅读「标准流程」章节，按步骤执行完整流程
5. 遇到问题时查阅「错误码体系」章节

### 7.3 进阶路径（熟练用户）

1. 使用 `--format json` 集成到自动化管线
2. 自定义节点依赖关系并运行 `--validate` 验证
3. 结合 `--budget` 输出进行成本敏感性分析
4. 将协作协议与团队实际工作流对接
5. 定期运行 `--selftest` 确保功能一致性

---

## 八、核心数据模型

### 8.1 角色定义模型

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| role_id | string | 是 | 角色唯一标识 |
| role_name | string | 是 | 角色名称 |
| responsibilities | array | 是 | 职责列表 |
| inputs | array | 是 | 输入格式 |
| outputs | array | 是 | 输出格式 |
| collaborators | array | 是 | 协作对象 |

### 8.2 节点定义模型

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| node_id | string | 是 | 节点唯一标识 |
| node_name | string | 是 | 节点名称 |
| description | string | 是 | 节点描述 |
| dependencies | array | 否 | 前置依赖节点 |
| template | string | 是 | 交付物模板名称 |

### 8.3 预算参数模型

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| stage | string | 是 | 制作阶段 |
| cost_item | string | 是 | 成本项目 |
| unit_cost | number | 是 | 单位成本 |
| quantity | number | 是 | 数量 |
| total | number | 是 | 小计 |

---

## 九、使用建议

### 9.1 最佳实践

- 每次环境变更后立即运行 `--selftest`
- 在修改节点依赖后运行 `--validate`
- 程序化处理时优先使用 `--format json`
- 预算估算时结合实际市场行情调整参数

### 9.2 注意事项

- 本 Skill 不生成创意内容，仅提供流程编排
- 预算输出为估算值，不构成财务建议
- 所有数据均为内置，不访问外部服务

---

## 用户协议

<!-- user-agreement-injected -->

**生效日期**：2026 年 8 月 10 日

**1. 责任承担**

使用者自行承担使用本 Skill 的全部责任。本 Skill 提供的所有输出（包括但不限于流程定义、预算估算、协作协议）仅供参考，不构成任何形式的专业建议。使用者应结合自身实际情况，对输出内容进行独立判断和验证。

**2. 禁止反向工程**

使用者不得对本 Skill 进行反向工程、反编译、反汇编，或试图提取源代码、算法、数据结构等底层实现。不得移除、修改或遮蔽本 Skill 中的任何版权声明、商标标识或法律声明。

**3. 合规使用**

使用者应确保使用本 Skill 的行为符合适用法律法规。不得将本 Skill 用于任何非法目的或侵犯第三方权益的行为。

**4. 免责声明**

本 Skill 按"现状"提供，不附带任何明示或暗示的保证，包括但不限于适销性、特定用途适用性和非侵权保证。在任何情况下，Skill 作者均不对因使用本 Skill 而产生的任何索赔、损害或其他责任负责。

---

## 许可证（License）

<!-- professional-license-embedded -->

**MIT License**

版权所有 (c) 2026 原创作者（自持版权）

特此免费授予任何获得本软件及相关文档文件（以下简称"软件"）副本的人士，不受限制地处理本软件，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或销售软件副本的权利，并允许向提供软件的人士授权这样做，但须满足以下条件：

上述版权声明和本许可声明应包含在软件的所有副本或实质性部分中。

本软件按"现状"提供，不附带任何明示或暗示的保证，包括但不限于适销性、特定用途适用性和非侵权保证。在任何情况下，作者或版权持有人均不对因使用本软件而产生的任何索赔、损害或其他责任负责，无论是在合同诉讼、侵权或其他方面。

---

*本 Skill 由 AI 辅助生成，仅供参考。使用前请阅读相关文档。*
