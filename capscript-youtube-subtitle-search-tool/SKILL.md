---
slug: capscript-youtube-subtitle-search-tool
name: capscript-youtube-subtitle-search-tool
displayName: 字幕检索 时间轴定位 关键词过滤
description: 将字幕文件转为结构化检索结果，支持时间轴定位与关键词过滤。
version: 1.0.0
license: MIT
source_project: original
source_url: 
copyright_holder: 原创作者（自持版权）
ai_generated: true
ai_tools: ["DeepSeek"]
disclaimer: 本Skill由AI辅助生成，提供使用指导和最佳实践。使用前请阅读相关文档。
author: CapScript Studio
agent_created: true
trigger_words: ["视频字幕", "字幕检索", "字幕搜索", "youtube subtitle", "字幕翻译", "字幕定位", "字幕过滤"]
---

> 本内容由 AI 生成，仅供学习参考
<!-- ai-generated-notice -->

# capscript-youtube-subtitle-search-tool 技能文档

## 一、能力边界（一页纸速查卡）

### 1.1 能做什么

| 能力项 | 说明 | 输入要求 | 输出格式 |
|--------|------|----------|----------|
| 字幕结构化 | 将原始字幕文本解析为带时间轴的结构化条目 | 标准 SRT / VTT 格式文本 | JSON 数组，每条含 `id`、`start`、`end`、`text` 字段 |
| 关键词过滤 | 按关键词或短语筛选字幕条目 | 关键词列表（支持正则） | 过滤后的 JSON 数组，保留原时间轴 |
| 时间轴定位 | 按时间点或时间段截取字幕片段 | 起始时间 + 结束时间（秒） | 该时间段内的字幕条目 |
| 批量处理 | 对多个字幕文件执行相同操作 | 文件列表 + 统一参数 | 每个文件对应一个输出文件 |

### 1.2 不能做什么

- 不能直接下载 YouTube 视频或字幕文件（需用户自行准备输入文件）
- 不能翻译字幕内容（仅支持检索与过滤，翻译需配合其他工具）
- 不能处理加密或损坏的字幕文件
- 不能识别非文本格式（如纯图片字幕、手写字幕）

### 1.3 适用对象

- 需要快速定位视频中特定内容片段的视频编辑者
- 需要从长视频中提取关键信息的课程学习者
- 需要对字幕进行关键词归档的内容管理者

---

## 二、触发方式

### 2.1 触发词

当用户输入以下任一词汇或短语时，本技能将被激活：

| 触发词 | 使用场景示例 |
|--------|--------------|
| 视频字幕 | "帮我处理这个视频字幕文件" |
| 字幕检索 | "检索字幕里所有提到'机器学习'的地方" |
| 字幕搜索 | "搜索字幕中关于'安装步骤'的内容" |
| youtube subtitle | "Process this youtube subtitle file" |
| 字幕翻译 | "把字幕翻译成中文"（仅触发，翻译需外部工具） |
| 字幕定位 | "找到第3分钟到第5分钟的字幕内容" |
| 字幕过滤 | "过滤掉所有包含'嗯'、'啊'的填充词" |

### 2.2 场景映射表

| 用户实际需求 | 大白话表达 | 技能响应动作 |
|--------------|------------|--------------|
| 找视频里某段话 | "我想知道视频里什么时候说了XXX" | 执行关键词过滤 + 时间轴定位 |
| 提取某时间段内容 | "把视频前10分钟的字幕给我" | 执行时间轴截取 |
| 整理字幕归档 | "把字幕按主题分类" | 执行关键词过滤 + 分组输出 |
| 检查字幕完整性 | "看看字幕有没有缺行" | 执行结构化解析 + 完整性校验 |

---

## 三、标准流程

### 3.1 前置条件

| 条件项 | 要求 | 检查方式 |
|--------|------|----------|
| 输入文件格式 | SRT 或 VTT 格式，UTF-8 编码 | 文件头检查（SRT 以序号开头，VTT 含 `WEBVTT` 标记） |
| 文件命名 | 统一命名规范，如 `video1.srt`、`video2.srt` | 目视检查 |
| 文件完整性 | 文件可正常打开，无乱码 | 试读前 10 行 |
| 环境依赖 | Python 3.8+ 或 Node.js 14+ | 命令行执行 `python --version` 或 `node --version` |

### 3.2 执行步骤

#### 步骤 1：准备输入

将待处理的字幕文件放入同一工作目录，确认命名规范一致。例如：

```
/workspace/
├── video1.srt
├── video2.srt
└── video3.srt
```

#### 步骤 2：试运行（单样本验证）

使用单个文件执行一次完整流程，核对输出字段与格式是否符合预期。

**命令示例（Python）：**

```python
from subtitle_parser import parse_srt

# 解析单个文件
result = parse_srt("video1.srt")
print(result[:3])  # 打印前3条，检查字段完整性
```

**预期输出示例：**

```json
[
  {"id": 1, "start": 0.5, "end": 3.2, "text": "大家好，欢迎观看本期视频"},
  {"id": 2, "start": 3.5, "end": 6.8, "text": "今天我们要讨论的话题是..."},
  {"id": 3, "start": 7.0, "end": 10.5, "text": "首先让我们来看一个例子"}
]
```

**校验点：**
- `id` 为递增整数
- `start` 和 `end` 为浮点数（单位：秒）
- `text` 为字符串，无多余空白字符

#### 步骤 3：批量执行

确认单样本无误后，对全量数据执行处理。保留原始文件备份。

**命令示例：**

```python
import glob

files = glob.glob("*.srt")
for f in files:
    result = parse_srt(f)
    output_file = f.replace(".srt", "_parsed.json")
    save_json(result, output_file)
    print(f"已处理: {f} -> {output_file}")
```

#### 步骤 4：校验结果

抽查输出条目，核对关键字段与源数据一致。

**校验清单：**

| 校验项 | 方法 | 通过标准 |
|--------|------|----------|
| 条目数量 | 对比源文件字幕块数量 | 数量一致 |
| 时间轴连续性 | 检查相邻条目时间是否重叠 | 无重叠或重叠 < 0.5 秒 |
| 文本完整性 | 随机抽取 5 条对比原文 | 文本一致，无截断 |

### 3.3 输出规范

输出文件为 JSON 格式，结构如下：

```json
{
  "source_file": "video1.srt",
  "parse_date": "2025-01-15T10:30:00Z",
  "total_entries": 152,
  "entries": [
    {"id": 1, "start": 0.5, "end": 3.2, "text": "..."},
    ...
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `source_file` | string | 源文件名 |
| `parse_date` | string | 解析时间（ISO 8601 格式） |
| `total_entries` | integer | 字幕条目总数 |
| `entries` | array | 字幕条目数组 |

---

## 四、置信度门控

### 4.1 信息不足时的处理

当输入信息不完整或存在歧义时，使用 `[需核实:字段]` 占位符，不编造数据。

| 场景 | 处理方式 | 示例 |
|------|----------|------|
| 时间格式不标准 | 标记为 `[需核实:时间格式]`，跳过该条 | `{"id": 5, "start": "[需核实:时间格式]", "end": "[需核实:时间格式]", "text": "..."}` |
| 文本编码异常 | 标记为 `[需核实:编码]`，保留原始字节 | `{"id": 8, "start": 12.0, "end": 15.5, "text": "[需核实:编码]\\xe4\\xb8\\xad"}` |
| 关键词匹配歧义 | 输出所有可能结果，标注匹配类型 | 在结果中增加 `match_type` 字段：`exact` / `fuzzy` |

### 4.2 置信度分级

| 置信度等级 | 判定标准 | 输出行为 |
|------------|----------|----------|
| 高（≥95%） | 源文件格式标准，解析无异常 | 正常输出，无额外标记 |
| 中（80-94%） | 存在少量格式不规范条目 | 输出结果，附带警告列表 |
| 低（<80%） | 文件格式严重不规范 | 拒绝输出，提示用户检查文件 |

---

## 五、错误码体系

| 错误码 | 错误描述 | 提示话术 | 修正步骤 |
|--------|----------|----------|----------|
| `E001` | 文件不存在 | "未找到指定的字幕文件，请检查文件路径" | 1. 确认文件路径正确；2. 检查文件名大小写；3. 确认文件已放入工作目录 |
| `E002` | 文件格式不支持 | "仅支持 SRT 和 VTT 格式的字幕文件" | 1. 确认文件扩展名；2. 转换格式后重试 |
| `E003` | 文件编码异常 | "文件编码不是 UTF-8，可能存在乱码" | 1. 使用文本编辑器另存为 UTF-8 编码；2. 重新执行 |
| `E004` | 时间轴解析失败 | "时间轴格式不符合标准，无法解析" | 1. 检查时间轴格式（应为 `HH:MM:SS,mmm` 或 `HH:MM:SS.mmm`）；2. 手动修正后重试 |
| `E005` | 关键词格式错误 | "关键词列表格式不正确，应为字符串或字符串数组" | 1. 检查关键词输入格式；2. 使用 JSON 数组格式传入 |
| `E006` | 批量处理中断 | "批量处理在第 N 个文件时中断" | 1. 查看错误日志；2. 修复问题文件；3. 从断点继续处理 |

---

## 六、FAQ 反模式

### 6.1 常见坑与反模式对照

| 常见坑 | 反模式（错误做法） | 正确做法 |
|--------|---------------------|----------|
| 时间轴偏移 | 直接使用原始时间轴，未考虑视频裁剪 | 确认视频是否经过剪辑，如有偏移需手动调整 |
| 关键词误匹配 | 使用过于宽泛的关键词（如"的"、"是"） | 使用具体名词或短语，或启用正则精确匹配 |
| 批量处理覆盖原文件 | 直接覆盖源文件 | 保留原始文件备份，输出到独立目录 |
| 忽略格式警告 | 忽略解析警告，直接使用结果 | 检查警告列表，确认不影响使用后再继续 |
| 跨平台时间格式混淆 | 混用 `,` 和 `.` 作为毫秒分隔符 | 统一使用 SRT 标准（逗号）或 VTT 标准（点号） |

### 6.2 反模式示例

**反模式 1：忽略编码问题**

```
❌ 错误：直接解析，忽略乱码提示
✅ 正确：先检查编码，转换后再解析
```

**反模式 2：关键词过于宽泛**

```
❌ 错误：搜索"学习"匹配到所有包含"学习"的条目
✅ 正确：搜索"机器学习"或使用正则 `\b机器学习\b` 精确匹配
```

**反模式 3：不校验批量结果**

```
❌ 错误：批量处理完成后直接使用，不抽查
✅ 正确：随机抽取 5-10 条，核对时间轴和文本完整性
```

---

## 七、渐进式披露

### 7.1 速查卡（30 秒上手）

```
1. 放入字幕文件（SRT/VTT）
2. 运行：python parse.py your_file.srt
3. 获取：your_file_parsed.json
4. 过滤：python filter.py your_file_parsed.json "关键词"
5. 定位：python slice.py your_file_parsed.json 60 120
```

### 7.2 新手路径（首次使用）

1. 阅读「能力边界」了解技能范围
2. 准备一个 SRT 格式的测试文件
3. 按照「标准流程」步骤 1-2 执行试运行
4. 查看输出 JSON 结构，确认理解字段含义
5. 尝试使用关键词过滤功能

### 7.3 进阶路径（熟练使用）

1. 掌握批量处理与错误恢复
2. 使用正则表达式进行复杂关键词匹配
3. 结合时间轴定位实现视频片段提取
4. 自定义输出格式（如 CSV、Markdown 表格）
5. 编写自动化脚本，集成到视频处理流水线

---

## 八、参数参考表

### 8.1 解析参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--input` | string | 必填 | 输入字幕文件路径 |
| `--output` | string | 自动生成 | 输出 JSON 文件路径 |
| `--format` | string | `auto` | 输入格式（`srt` / `vtt` / `auto`） |
| `--encoding` | string | `utf-8` | 文件编码 |

### 8.2 过滤参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--keywords` | array | 必填 | 关键词列表 |
| `--regex` | boolean | `false` | 是否启用正则匹配 |
| `--case-sensitive` | boolean | `false` | 是否区分大小写 |
| `--match-all` | boolean | `false` | 是否要求匹配所有关键词（默认匹配任一） |

### 8.3 时间轴截取参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--start` | float | 必填 | 起始时间（秒） |
| `--end` | float | 必填 | 结束时间（秒） |
| `--include-boundary` | boolean | `true` | 是否包含边界条目 |

---

## 九、用户协议

<!-- user-agreement-injected -->

**使用本 Skill 即表示您同意以下条款：**

1. **责任承担**：使用者自行承担使用本 Skill 的全部责任。因使用本 Skill 导致的任何直接或间接损失，包括但不限于数据丢失、业务中断、法律纠纷等，本 Skill 作者及贡献者不承担任何责任。

2. **禁止反向工程**：禁止对本 Skill 进行反向工程、反编译、破解或试图提取源代码（除非适用法律允许）。

3. **合法使用**：使用者应确保使用本 Skill 的行为符合当地法律法规，不得用于侵犯他人知识产权、隐私权或其他合法权益的活动。

4. **无担保声明**：本 Skill 按"原样"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权保证。

5. **修改与分发**：使用者可以在遵守 MIT 许可证的前提下修改和分发本 Skill，但需保留原始版权声明。

---

## 十、许可证（License）

<!-- professional-license-embedded -->

**MIT License**

版权所有 (c) 2025 CapScript Studio

特此免费授予任何获得本软件及相关文档文件（以下简称"软件"）副本的人士处理软件的权限，包括不受限制地使用、复制、修改、合并、发布、分发、再许可和/或出售软件副本的权利，并允许向其提供软件的人士这样做，但须满足以下条件：

上述版权声明和本许可声明应包含在软件的所有副本或实质性部分中。

本软件按"原样"提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权保证。在任何情况下，作者或版权持有人均不对任何索赔、损害或其他责任负责，无论是在合同诉讼、侵权行为或其他方面，由软件或软件的使用或其他交易引起、产生或与之相关。

---

*本 Skill 由 AI 辅助生成，仅供参考。使用前请阅读相关文档。*
